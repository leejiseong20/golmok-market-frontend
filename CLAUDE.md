# 골목마켓 (프론트엔드)

동네 기반 중고거래 플랫폼. 취업 포트폴리오 프로젝트의 React 클라이언트.
백엔드는 별도 저장소다: [golmok-market](https://github.com/leejiseong20/golmok-market)

## 스택

- React 18 + Vite 5, JavaScript (TypeScript 아님)
- 상태 관리 라이브러리 없음. `useState` + `useSyncExternalStore`
- 라우터 없음. `App.jsx`의 `view` 상태로 홈/마이페이지를 전환
- CSS Modules + `index.css`의 CSS 변수(디자인 토큰)
- 테스트: `node:test`(API 클라이언트), Playwright(E2E)

## 작업 규칙

- 모든 응답은 한국어로.
- 코드 조각이 아니라 **완성된 파일 전체**로 제시한다.
- 왜 그렇게 설계했는지 짧게라도 근거를 붙인다. 면접에서 설명할 수 있어야 한다.
- 문제가 보이면 돌려 말하지 말고 직접 지적한다.
- 파일을 만들거나 고치기 전에 **무엇을 건드릴지 먼저 보여주고 확인을 받는다.**
- 작업이 끝나면 `npm run build`가 통과하는지 확인하고 결과를 보고한다.
- 커밋은 요청받았을 때만. 메시지는 한국어로 "무엇을, 왜". 푸시도 따로 요청받았을 때만.

## 지켜야 할 제약

- **API 명세는 백엔드 저장소의 `API-SPEC.md`가 기준이다.** 응답 형식을 추측하지 말고 명세를 확인한다. 명세와 다르게 동작하면 프론트에서 우회하지 말고 백엔드 문제로 알린다.
- **목업 데이터를 만들지 않는다.** 화면은 실제 API로만 그린다. (E2E 테스트의 모킹은 예외)
- **의존성을 함부로 추가하지 않는다.** 라우터, 상태 관리, UI 라이브러리 도입은 먼저 물어본다. 지금은 런타임 의존성이 `react`, `react-dom` 둘뿐이다.
- 사용자에게 보이는 문구는 한국어. 에러 메시지는 서버가 준 `message`를 그대로 쓴다.
- 토큰·비밀값을 코드에 적지 않는다. 환경값은 `VITE_` 접두사로 `.env`에 두고 커밋하지 않는다.
- 새 화면을 만들면 로딩·에러·빈 상태 세 가지를 모두 처리한다.

## 실행

백엔드가 `http://localhost:8080`에 떠 있어야 한다.

```bash
npm install
npm run dev        # http://127.0.0.1:5173
npm run build      # 프로덕션 번들
npm test           # API 클라이언트 단위 테스트
npm run test:e2e   # Playwright. API를 모킹하므로 백엔드 없이 돈다
```

데모 계정: `demo4@golmok.test` / `Golmok123!` (백엔드 `scripts/seed_demo_data.sql`로 넣은 데이터)

## 파일

```
src/
  App.jsx            화면 조립 + 상태(동네/카테고리/정렬/검색/찜/화면 전환)
  api/client.js      fetch 래퍼. 토큰 보관, 401 처리, 재발급
  api/*.js           엔드포인트별 얇은 함수
  components/        화면 조각. 각각 같은 이름의 .module.css
  data/format.js     가격 · 상대시간 · 상태 라벨
```

## 설계 결정 기록

왜 이렇게 했는지. 나중에 뒤집으려 할 때 먼저 읽을 것.

- **세션은 `sessionStorage`에 둔다.** 탭을 닫으면 로그아웃된다. 공용 PC에서 토큰이 남지 않게 하려는 선택이다. 저장소 접근이 막힌 환경(비공개 모드)에서도 메모리로 동작한다.
- **선택한 동네는 `localStorage`에 둔다.** 세션과 달리 비로그인에도 유지돼야 하고, 민감한 값이 아니다.
- **토큰 재발급은 한 번만 보낸다.** 여러 요청이 동시에 만료되면 재발급 하나를 공유하고 나머지는 그 결과를 기다린다. 각자 재발급하면 서버가 rotation으로 이전 토큰을 폐기해 나머지가 로그아웃된다.
- **401 코드로 동작을 나눈다.** `EXPIRED_TOKEN`이면 재발급 후 재시도, `INVALID_TOKEN`·`UNAUTHORIZED`면 세션을 지운다. 네트워크 오류나 5xx에서는 토큰을 지우지 않는다.
- **찜은 낙관적 갱신을 하지 않는다.** 서버가 준 `{ isLiked, favoriteCount }`만 반영한다. 하트와 관심 수가 어긋난 채 남는 것보다 잠깐 늦는 편이 낫다.
- **상품 상세는 사용자 동작으로만 호출한다.** 상세 조회가 조회수를 올리므로, StrictMode의 effect 재실행으로 두 번 세어지면 안 된다.
- **목록은 커서 페이징이다.** `nextCursor`를 해석하지 않고 그대로 돌려보낸다. "더 보기"로 이어 붙일 때 id로 중복을 걸러낸다.
- **요청은 화면이 바뀌면 `AbortController`로 취소한다.** 느린 이전 요청이 나중에 도착해 새 화면을 덮어쓰는 것을 막는다.
- **찜 버튼은 카드 버튼 바깥에 둔다.** `button` 안에 `button`은 유효하지 않은 HTML이라 브라우저마다 클릭 동작이 달라진다.
- **브레이크포인트는 880px 하나다.** 모바일 우선으로 쓰고 `@media (min-width: 880px)`에서 PC로 전환한다.

## 알려진 과제

- **라우터가 없다.** 상세·마이페이지가 URL에 남지 않아 새로고침하면 홈으로 돌아가고 링크 공유도 안 된다. 화면이 더 늘면 `react-router` 도입을 검토한다.
- **E2E는 API를 모킹한다.** 백엔드와 실제로 맞는지는 검증하지 못한다. 마이페이지와 찜 토글은 아직 E2E에 없다.
- **접근성 점검을 하지 않았다.** 키보드 이동과 스크린리더 확인이 필요하다.
- **이미지 최적화가 없다.** 원본 URL을 그대로 쓴다. 목록에서 큰 이미지를 그대로 받는다.
- 백엔드가 `127.0.0.1`과 `localhost`를 모두 허용하지만, 배포 시에는 실제 도메인을 백엔드 `CORS_ALLOWED_ORIGINS`에 넣어야 한다. 빠지면 조회는 되는데 로그인만 403이 난다.
