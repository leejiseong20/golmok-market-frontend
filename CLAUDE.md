# 골목마켓 (프론트엔드)

동네 기반 중고거래 플랫폼. 취업 포트폴리오 프로젝트의 React 클라이언트.
백엔드는 별도 저장소다: [golmok-market](https://github.com/leejiseong20/golmok-market)

## 스택

- React 18 + Vite 5, JavaScript (TypeScript 아님)
- 채팅 실시간: `@stomp/stompjs` (STOMP over WebSocket)
- 상태 관리 라이브러리 없음. `useState` + `useSyncExternalStore`
- 라우터: `react-router`(2026-09-17 승인). 주소 규칙은 `src/routes.js` 한 곳에 둔다
- CSS Modules + `index.css`의 CSS 변수(디자인 토큰)
- 테스트: `node:test`(API 클라이언트), Playwright(E2E)
- PWA: 직접 쓴 서비스 워커(`public/sw.js`) + manifest. 플러그인 없음

## 작업 규칙

- 모든 응답은 한국어로.
- 코드 조각이 아니라 **완성된 파일 전체**로 제시한다.
- 왜 그렇게 설계했는지 짧게라도 근거를 붙인다. 면접에서 설명할 수 있어야 한다.
- 문제가 보이면 돌려 말하지 말고 직접 지적한다.
- 파일을 만들거나 고치기 전에 **무엇을 건드릴지 먼저 보여주고 확인을 받는다.**
- 작업이 끝나면 `npm run build`가 통과하는지 확인하고 결과를 보고한다.
- 커밋은 요청받았을 때만. 메시지는 한국어로 "무엇을, 왜". 푸시도 따로 요청받았을 때만.

## 지켜야 할 제약

- **API 명세는 백엔드 저장소의 `API-SPEC.md`가 기준이다.** 응답 형식을 추측하지 말고 명세를 확인한다. 명세와 다르게 동작하면 프론트에서 우회하지 말고 백엔드 문제로 알린다.
- **목업 데이터를 만들지 않는다.** 화면은 실제 API로만 그린다. (E2E 테스트의 모킹은 예외)
- **의존성을 함부로 추가하지 않는다.** 라우터, 상태 관리, UI 라이브러리 도입은 먼저 물어본다. 지금 런타임 의존성은 `react`, `react-dom`, `@stomp/stompjs`(채팅 실시간), `react-router`(라우팅) 넷이다(뒤 둘은 2026-09-17 승인).
- 사용자에게 보이는 문구는 한국어. 에러 메시지는 서버가 준 `message`를 그대로 쓴다.
- 토큰·비밀값을 코드에 적지 않는다. 환경값은 `VITE_` 접두사로 `.env`에 두고 커밋하지 않는다.
- 새 화면을 만들면 로딩·에러·빈 상태 세 가지를 모두 처리한다.

## 실행

백엔드가 `http://localhost:8080`에 떠 있어야 한다.

```bash
npm install
npm run dev        # http://127.0.0.1:5173
npm run build      # 프로덕션 번들
npm test           # API 클라이언트 · 채팅 소켓 · 후기 · 알림 · 주소 규칙 단위 테스트
npm run test:e2e   # Playwright. API를 모킹하므로 백엔드 없이 돈다
npm run test:e2e:live # 실제 MySQL 백엔드 실행 필요. 별도 검증 계정으로 상품 쓰기 흐름 확인
```

데모 계정: `demo4@golmok.test` / `Golmok123!` (백엔드 `scripts/seed_demo_data.sql`로 넣은 데이터)

## 파일

```
src/
  App.jsx            화면 조립. 주소 → 페이지·모달, 동네·찜·상세 상태
  routes.js          주소 만들기·해석(상품·프로필·채팅방·마이페이지 탭·홈 쿼리), 알림 경로 허용 목록
  api/client.js      fetch 래퍼. 토큰 보관, 401 처리, 재발급
  api/*.js           엔드포인트별 얇은 함수
  api/chatSocket.js  채팅 실시간 연결 관리(재연결, 토큰 만료 시 재발급 공유)
  components/        화면 조각. 각각 같은 이름의 .module.css
  data/format.js     가격 · 상대시간 · 상태 라벨
```

## 설계 결정 기록

- **2026-09-22 푸시 해제 순서를 변경.** 기존 서버 삭제 우선 및 전체 해제 작업을 1.5초만 기다리던 결정을 뒤집었다. 브라우저 unsubscribe를 먼저 완료해야 이전 계정 알림을 막을 수 있다. 서버 삭제만 제한 시간을 두고, 브라우저 해제 실패 시 로그아웃·화면 이동을 성공 처리하지 않는다.
- **pushManager 가 없으면 "구독 없음"으로 본다(2026-09-22).** iOS 는 홈 화면 앱에서만 `pushManager` 를 주고, Safari·Chrome 탭에서는 서비스 워커 등록이 있어도 `registration.pushManager` 가 없다. 이를 부르면 TypeError 가 나는데, 위 결정(브라우저 해제 실패 시 로그아웃 실패)과 겹쳐 **아이폰 크롬에서 로그아웃이 되지 않았다**(사용자 보고). 구독할 수 없는 곳이라 해제할 구독도 없다 — `currentSubscription` 이 null 을 돌려준다.
- **계정과 구독 작업을 같은 큐로 처리.** main.jsx에서 세션 변화를 구독하고 세대 번호로 이전 계정의 늦은 저장을 폐기한다. localStorage의 golmok.push.owner에는 계정 id만 남긴다(키·토큰 아님). 현재 계정과 다르거나 소유자 불명이면 구독을 해제하고 새 계정이 명시적으로 다시 켠다. 동일 계정 재시작은 구독을 유지한다.
- **켜짐은 브라우저 존재 여부에서 서버 확인 기준으로 변경.** 설정 진입 시 VAPID 공개키와 서버 재등록을 확인한다. 저장 실패 시 구독도 해제하며 오류와 재시도 버튼을 보인다. Safari 사용자 동작 제약 때문에 권한 요청은 큐 진입 전에 버튼 처리에서 즉시 시작한다.
- **캐시는 부가 기능.** 열기·읽기·쓰기 오류가 정상 네트워크 응답을 버리지 않게 한다. 오프라인이며 캐시도 실패하면 503 안내 화면을 반환한다. 응답 본문 읽기 실패는 ApiError로 전달하고 취소는 AbortError를 유지한다.
- **알림 경로는 URL 파싱과 앱 경로 허용 목록을 함께 검사.** 동일 출처 여부와 원래 경로 일치를 확인하여 역슬래시·외부 주소·정규화 경로·쿼리 우회를 홈으로 돌린다.


- **2026-09-17 라우터 도입: 주소가 화면을 정한다.** `/`(홈, `?q=&category=&sort=`), `/products/:id`·`/users/:id`(모달), `/chat`, `/chat-rooms/:id`, `/my`·`/my/:tab`, 그 외 NotFound. 알림 `targetUrl` 과 같은 형식이라 알림은 경로 그대로 이동한다. 동네 선택은 개인 설정이라 주소에 넣지 않고 `localStorage` 에 둔다. 로그인·동네 선택·알림함·등록/수정 창도 주소에 넣지 않는다(공유 대상이 아니고, 작성 중 뒤로가기로 입력이 날아가면 안 된다).
- **상세·프로필은 "보던 화면 위의 모달"(background location)이다.** 열 때 보던 페이지의 location 을 `state.background` 로 넘기고 `<Routes location={background}>` 로 페이지를, 실제 주소로 모달을 그린다. 모달을 열고 닫아도 아래 목록·더 보기·스크롤이 유지된다. 주소로 바로 들어오면 background 가 없어 홈 위에 띄우고, 닫으면 홈으로 기록을 바꿔치기한다. 앱 안에서 열었으면 닫기가 뒤로가기다.
- **상세 조회는 기록 항목(`location.key`)마다 한 번만 보낸다.** 상세 GET 이 조회수를 올리는데, 주소 기반이 되면서 "사용자 동작에서만 호출" 규칙을 지킬 수 없게 됐다. 대신 key 별로 요청 promise 를 기억해 StrictMode 의 effect 재실행은 첫 요청을 기다리고(그래서 abort 하지 않는다), 프로필을 열었다 뒤로가기로 돌아온 상세도 다시 세지 않는다. 새로 들어오면 새 key 라 새 조회로 센다. 저장 직후에는 저장 응답을 `state.product` 로 넘겨 조회하지 않는다. 사용자 변경 감지는 전용 ref 를 현재 사용자로 초기화한다(세션 복원 직후를 변경으로 오인하면 상세를 두 번 받는다).
- **홈 목록 조건(검색어·카테고리·정렬)은 쿼리로 둔다.** 검색 결과 공유와 뒤로가기로 이전 검색 결과 복귀가 된다. 기본값은 쿼리에서 빼 같은 조건이 늘 같은 주소가 되게 하고, 모르는 값은 에러 대신 기본값으로 되돌린다.
- **마이페이지 목록은 어느 탭의 데이터인지(`list.tab`)를 함께 둔다.** 탭이 주소로 바뀌면서 뒤로가기로 탭이 바뀐 첫 렌더에 이전 탭 항목을 새 탭 카드로 그려 터질 수 있었다(클릭 때 목록을 비우던 방식으로는 막지 못한다). 탭이 다르면 비어 있는 것으로 본다.
- **배포 시 SPA 대체 경로가 필요하다.** `vercel.json` 에 `/api/` 가 아닌 모든 경로를 `index.html` 로 보내는 rewrite 를 뒀다. 없으면 `/products/12` 로 바로 들어오거나 새로고침하면 404 다.
- **2026-09-17 리뷰·매너온도**: 거래 기준 작성 API를 채팅방 거래 줄(양쪽)·구매내역 카드(구매자)에 연결했다. 작성 가능 여부는 서버의 `tradeActions.review`·`canReview`만 사용한다. 기한 없음, 즉시 공개, 수정·삭제 없음, 시스템 메시지 없음은 승인된 정책이다.
- **공용 ReviewForm과 ReviewList를 사용한다.** 폼은 점수를 직접 고르게 하고 500자 제한·등록 중 중복 클릭 방지·완료 안내를 제공한다. 다른 탭에서 이미 작성했으면 ALREADY_REVIEWED를 완료 상태로 바꾸고 닫을 때 재조회한다. POST 성공과 후속 재조회 실패를 혼동해 다시 작성하지 않도록 완료 화면을 분리했다.
- **상대 프로필은 별도 모달이다.** 상품 상세의 판매자·채팅 상대 이름에서 공개 프로필과 받은 후기를 연다. 마이페이지에는 받은 후기 탭을 추가했다. 서로 다른 형태의 목록을 전환할 때 이전 목록을 즉시 비우고, 받은 후기 탭에서는 구매 목록 API를 호출하지 않는다.
- **후기는 REST로 갱신한다.** 시스템 메시지·별도 소켓 이벤트가 없으므로 상대가 후기 작성 후 이미 열려 있던 화면은 다시 열거나 새로고침해야 최신 온도가 보인다.
- **알림 뱃지 숫자는 App 이 관리한다.** 로그인·소켓 (재)연결 때 `unread-count` 로 서버 값에 맞추고, 그 사이에는 `NOTIFICATION` 이벤트마다 1 올린다. 읽음은 서버 성공 뒤에만 줄인다(찜과 같은 이유로 낙관적 갱신 없음). 끊긴 동안 온 알림은 이벤트로 다시 오지 않으므로 재연결 때 다시 센다.
- **알림 이동은 `routes.isAppPath` 허용 목록을 통과한 경로만 따른다.** 외부 주소(`https://…`, `//…`)나 모르는 경로는 이동하지 않는다(열린 리다이렉트 방지). 서버가 새 경로를 추가해도 화면이 깨지지 않고 알림함만 닫힌다.
- **프로필 저장 뒤 세션의 사용자 정보도 바꾼다(`client.updateUser`).** 헤더 닉네임은 로그인 때 저장한 세션에서 읽기 때문이다. 그 사이 로그아웃했거나 다른 계정이면 무시해 늦게 끝난 요청이 새 세션을 덮지 않게 한다. 저장 응답이 갱신된 내 정보라 마이페이지는 다시 조회하지 않는다.
- **인기 검색어는 부가 정보라 모바일 칩은 실패·빈 결과면 자리를 차지하지 않게 숨긴다.** PC 안내 패널은 제목이 있는 자리라 로딩·에러(다시 시도)·빈 상태를 모두 보인다. 누르면 홈 쿼리(`q`)만 바꿔 검색 결과 주소가 공유·뒤로가기와 같은 규칙을 따른다. 검색 중에는 결과에 집중하도록 칩을 숨긴다.
- **프로필 사진은 `Avatar` 컴포넌트 하나로 그린다.** 사진이 없거나 불러오지 못하면 닉네임 첫 글자다. 여섯 곳에 같은 로직이 흩어지지 않게 모았고, 실패한 주소를 기억해 사진을 바꾸면 다시 시도한다. 옆에 닉네임이 늘 함께 있어 보조기기에는 읽히지 않게 둔다. 채팅 목록은 어떤 물건의 대화인지가 더 중요해 상품 사진을 크게 두고 상대 사진은 닉네임 옆에 작게 붙인다.
- **프로필 수정 창은 닉네임 오류(중복·길이)를 입력칸 옆에, 나머지는 아래에 서버 문구 그대로 보인다.** 사진은 고르는 즉시 업로드하고, 업로드·저장 중에는 닫기와 중복 제출을 막는다. 바뀐 것이 없으면 저장 버튼을 끈다.
- **알림을 누르면 읽음 요청을 보내고 응답을 기다리지 않고 이동한다.** 읽음이 실패하면 다음에 알림함을 열 때 안 읽음으로 다시 보인다.

- **배포는 Vercel, `/api/*`는 rewrites 로 백엔드에 전달한다.** API 주소를 환경변수로 바꿔 직접 호출하면, 업로드 이미지 URL이 `/api/images/...` 상대 경로라 Vercel 도메인으로 요청돼 전부 깨진다. rewrites 를 쓰면 코드 변경 없이 API와 이미지가 모두 같은 출처로 동작한다. 단, 백엔드의 `CORS_ALLOWED_ORIGINS`에는 Vercel 주소가 반드시 있어야 한다(브라우저 Origin 이 그대로 전달된다).

- **상품 사진은 가로 한 줄에서 끌어 옮긴다(`PhotoSorter.jsx`, 2026-09-18).** 사진마다 ← → 버튼을 두니 두 장만 넘어도 화면이 아래로 길어졌다. 맨 앞이 대표 사진이고 "대표" 표시를 붙인다. 마우스와 손가락을 같은 코드로 다루려고 포인터 이벤트를 쓴다(HTML5 드래그는 모바일에서 동작하지 않는다). 손가락은 길게 눌러야(220ms) 끌기가 시작된다. 바로 시작하면 줄을 좌우로 넘기려던 손짓이 사진 이동이 된다. 끌기만 두면 키보드로는 순서를 바꿀 수 없어, 사진에 초점을 두고 ← → 키로도 옮기고 결과를 `aria-live` 로 읽어 준다. 순서 계산은 순수 함수(`data/reorder.js`)로 빼서 테스트한다(맞바꾸기가 아니라 끼워 넣기다). 끌기 도중 key 가 바뀌면 요소가 다시 만들어져 포인터 붙잡기가 끊기므로 key 는 사진 주소다.
- **끌고 있는 사진은 화면에 고정(`position: fixed`)해서 흐름에서 뺀다.** 줄 안에 둔 채 아래로 옮기면 줄의 스크롤 영역이 늘어나 세로 스크롤바가 생긴다(가로 스크롤을 켜면 세로도 자동으로 스크롤 가능해진다). 칸(`li`)은 크기가 고정이라 사진이 빠져도 자리가 유지되고, 빈자리는 점선으로 보여 준다. 기준점이 고정이라 순서가 바뀌어도 위치를 다시 계산할 필요가 없다.
- **끌 때 사진이 튀지 않게 하는 두 가지.** ① 순서가 바뀌면 끌던 사진도 새 칸으로 옮겨지므로, 그리기 전에(`useLayoutEffect`) 포인터 위치로 다시 맞춘다. 안 하면 한 칸만큼 순간이동한다. ② 자리 판정은 **끌기 시작할 때 찍어 둔 칸 위치**로 한다. 칸들이 미끄러지는 중의 순간 위치로 계산하면 판정이 흔들린다. 줄 위아래로 60px 넘게 벗어나면 자리를 바꾸지 않는다(손이 흔들렸다고 맨 뒤로 날아가면 안 된다).
- **끌 때 파랗게 선택되는 것은 `pointerdown` 의 기본 동작을 막고 `user-select: none` 으로 없앤다.** 이미지에는 `-webkit-user-drag: none` 도 준다. 브라우저 기본 이미지 끌기(반투명 유령 이미지)가 우리 애니메이션과 겹치기 때문이다.
- **입력 검증은 서버로 보내는 값(다듬은 값)으로 한다(2026-09-20).** 입력칸의 `minLength` 는 공백까지 세는데 요청에는 `trim()` 한 값을 보낸다. 그래서 "짧음······"(공백 포함 10자)은 브라우저 검사를 통과하고 서버에서 되돌아왔다. 실제 갤럭시에서 이 차이로 등록이 막혔고, 브라우저에서 `checkValidity()` 가 true 를 돌려주는 것까지 확인했다.
- **서버가 준 필드 오류는 해당 입력칸 아래에 붙인다.** 폼 맨 아래에 "description: ..." 처럼 영어 필드명으로 한 줄 뜨면 어디를 고칠지 알 수 없다. 입력칸이 있는 필드(`INLINE`)는 옆에 붙이고 `aria-invalid`·`aria-describedby` 로 연결하며, 입력칸이 없는 필드(사진 등)만 아래 목록에 한국어 이름(`LABELS`)으로 남긴다. 모르는 필드 이름은 그대로 보여 준다(서버가 필드를 늘려도 화면이 깨지지 않는다).
- **신고·차단은 창 하나(`ReportForm`)와 흐름 하나(App 의 `blockPerson`)로 모은다(2026-09-21).** 상세("이 게시글 신고하기")·프로필(신고하기·차단하기)·채팅방(점 세 개 메뉴) 어디서 열든 같은 창과 같은 확인 문구를 쓴다. 차단 뒤에는 목록·뱃지를 서버에 다시 묻는다(화면에서 항목을 직접 지우지 않는다 — 찜과 같은 이유). 신고 완료 화면에서 바로 차단할 수 있게 한다(신고한 뒤 그 사람을 계속 마주치고 싶지 않은 경우가 많다).
- **알림·내 동네·차단·로그아웃·탈퇴는 설정 화면(`/settings`, `SettingsPage`)에 모은다(2026-09-21).** 마이페이지에 이것들이 모두 붙어 화면이 복잡했다(사용자 요청). 마이페이지에는 프로필과 네 개의 탭 목록만 남기고, 프로필 옆 톱니바퀴로 설정에 들어간다. 모달이 아니라 화면인 이유: 뒤로가기로 자연스럽게 돌아오고, 동네 인증처럼 긴 내용도 넉넉하게 보인다. 주소는 `/my/:tab` 과 겹치지 않게 `/settings` 로 따로 뒀고(탭이 아니다), 하단 탭은 "나의 골목"을 켠다. 순서는 자주 쓰는 것 → 드문 것 → 되돌릴 수 없는 것(탈퇴는 맨 아래·위험 색).
- **헤더에는 로그아웃이 없다(2026-09-21).** 로그아웃은 설정 화면 한 곳이다(사용자 요청). 같은 글자의 버튼이 둘이면 어느 쪽을 눌러야 하는지 헷갈리고, 모바일에서는 헤더 쪽이 원래 숨겨져 있었다. 헤더 오른쪽은 비로그인일 때 "로그인"만 남는다.
- **설정의 뒤로 버튼은 앱 안에서 왔으면 기록을 한 칸 되돌리고, 주소로 바로 들어왔으면 나의 골목으로 바꿔치기한다.** 새 기록을 쌓으면 뒤로가기가 설정으로 되돌아온다. 판단은 react-router 가 기록에 남기는 `history.state.idx` 로 한다. 로그아웃하면 설정에 남을 이유가 없어 나의 골목(로그인 안내)으로 바꿔치기한다.
- **차단 목록은 마이페이지 탭이 아니라 설정의 한 줄(창)이다.** 탭이 다섯 개가 되면 375px 폰에서 넘친다(지금 네 개도 약 380px). 로그아웃·탈퇴처럼 가끔 여는 설정이라 그 옆이 자연스럽다.
- **신고는 드문 동작이라 조용히 둔다.** 상세에서는 글자 링크, 프로필에서는 후기 목록 아래, 채팅방에서는 메뉴 안이다. 프로필을 열자마자 "신고"가 크게 보이면 이웃을 의심하는 화면처럼 읽힌다.
- **응답 본문이 비어 있으면 204 가 아니어도 null 이다(`client.js`).** 차단 API 는 201 에 본문이 없는데, 예전 클라이언트는 204 만 빈 응답으로 보고 나머지는 JSON 으로 읽어 **성공한 차단이 "응답을 확인할 수 없습니다" 오류가 됐다**(화면 확인 전에 발견). 글자로 먼저 받고 비어 있지 않을 때만 JSON 으로 읽는다. 본문 없는 실패 응답은 여전히 오류다.
- **웹 푸시 켜기는 "휴대폰 알림" 한 줄이다(자리는 2026-09-21 마이페이지 프로필 아래 → 설정 화면으로 옮김. `PushToggle`, `push.js`, 2026-09-21).** 기기마다 켜고 끄는 설정이라 상태는 서버가 아니라 이 브라우저의 구독으로 판단한다. 서버에 키가 없으면(`enabled: false`) 줄 자체를 숨긴다. 권한 요청은 버튼 처리 안에서 가장 먼저 한다(사파리는 사용자 동작 밖에서 권한을 묻지 못한다). 서버 키가 바뀌면 옛 구독을 지우고 새로 만든다. 아이폰은 Safari 로 홈 화면에 추가한 앱에서만 받을 수 있어(iOS 16.4+) 그 밖에서는 설치 안내를 보인다(iOS Chrome 도 여기 해당).
- **서비스 워커는 받은 푸시를 반드시 알림으로 띄운다.** 띄우지 않으면 iOS·Chrome 이 구독을 끊거나 기본 알림을 대신 띄운다. 앱을 보고 있는 사람에게는 서버가 애초에 보내지 않는다. 알림을 누르면 앱 안 경로로만 이동한다(`safePath` — `//`·`https://` 는 `/` 로). 같은 채팅방 알림은 `tag` 로 겹친다.
- **로그아웃하면 이 기기의 푸시 구독부터 지운다.** 지우지 않으면 다음 사람이 이 기기를 써도 앞사람의 채팅 알림이 뜬다. 인증이 필요한 요청이라 로그아웃보다 먼저 한다. ~~1.5초 넘게 기다리지 않는다.~~ **(2026-09-22 뒤집음)** 브라우저 해제를 먼저 끝내고, 브라우저 해제가 실패하면 로그아웃도 실패로 본다 — 맨 위 2026-09-22 결정 참고. 서버에서 못 지워도 브라우저 구독은 지워지고, 남은 서버 행은 다음 발송 때 410 으로 정리된다.
- **상품 등록과 수정은 ProductForm 하나로 처리한다.** 인증한 동네만 선택하며 사진 1~10장과 순서를 관리한다. 사진 선택 즉시 업로드하고 저장 중에는 중복 제출·닫기를 막는다. 오류가 나면 입력은 유지한다.
- **공통 API 클라이언트가 FormData를 지원한다.** JSON 직렬화와 Content-Type 지정을 생략해 브라우저가 multipart 경계를 붙인다. 토큰 만료 재시도에도 동일한 파일 본문을 보존한다.
- **판매내역 탭은 TABS뿐 아니라 조회·카드·항목 키 분기도 확장했다.** 기존 "한 줄만 추가" 주석은 실제 코드와 달랐다. 상품 카드를 재사용하되 본인 판매내역에는 찜 버튼을 표시하지 않는다.
- **변경 성공 후 서버 응답과 목록 재조회로 화면을 맞춘다.** 상품 쓰기 응답을 상세에 바로 반영하고 홈·판매내역을 갱신한다. 탭·필터 변경 시 목록을 즉시 비우며 이전 요청을 취소한다.
- **현재 인증한 동네로 명시적 상품 이동을 허용한다.** 상세의 regionId로 수정 폼을 채운다. 이전 동네를 더 이상 인증하지 않은 경우 현재 인증 동네를 선택해 수정한다.
- **실제 서버 E2E를 기존 모킹 E2E와 분리했다.** playwright.live.config.js는 응답 대체 없이 별도 계정 가입·동네 인증 후 실제 UI를 검증한다. 비밀번호와 토큰이 기록되지 않도록 live trace는 끈다. 상품은 해당 실행에서 생성한 ID만 삭제하며 계정·파일은 남긴다.

왜 이렇게 했는지. 나중에 뒤집으려 할 때 먼저 읽을 것.

- **화면 모드(밝게·어둡게)는 `localStorage` 에 둔다(`theme.js`).** 고르기 전에는 시스템 설정(`prefers-color-scheme`)을 따르고, 한 번 고르면 그 선택이 시스템보다 우선한다. 선택은 `<html data-theme>` 로만 내려가고 색 교체는 CSS 가 한다(컴포넌트는 테마를 모른다). 첫 그림이 밝게 그려졌다 어두워지는 깜빡임을 막으려고 `index.html` 의 인라인 스크립트가 React 보다 먼저 값을 적용한다. 저장소 주입이 가능해 DOM 없이 테스트한다.
- **화면 모드는 설정 화면에서 고른다(2026-09-21, 사용자 요청).** 헤더의 해·달 버튼을 없애고 설정에 "시스템 · 밝게 · 어둡게" 세 칸을 뒀다. 버튼은 밝게 ↔ 어둡게만 오가서, 한 번 누르면 "기기 설정 따르기"로 돌아갈 방법이 없었다. 기기 설정이라 로그인과 무관하다 — 비로그인에게도 나의 골목에 "설정" 버튼을 두고, 비로그인 설정 화면에는 화면 모드만 보인다. `theme.toggle()` 은 이제 화면에서 쓰지 않지만 테스트가 있어 남겼다.
- **세션 저장 위치는 로그인할 때 고른다(2026-09-18).** "로그인 상태 유지"를 켜면 `localStorage`, 끄면 `sessionStorage`(탭을 닫으면 로그아웃)다. 원래는 공용 PC 를 생각해 늘 `sessionStorage` 였는데, 대부분 개인 기기에서 쓰는데도 매번 다시 로그인해야 하는 마찰이 컸다. 기본값은 켬이고 공용 PC 에서는 끄면 된다. 세션은 언제나 한 곳에만 둔다(고른 쪽에 쓰고 다른 쪽은 지운다). 토큰 재발급도 처음 복원한 저장소에 이어 쓴다. 저장소 접근이 막힌 환경(비공개 모드)에서도 메모리로 동작한다.
- **지나가는 알림은 토스트 하나로 모은다(`toast.js`, `Toaster.jsx`).** "저장했어요"·"끌어올렸어요" 같은 결과가 화면마다 다른 자리(모달 안 문구, 홈 상단 빨간 줄)에 떠 있었다. 성공·실패는 색이 아니라 왼쪽 띠로도 구분하고, `aria-live` 로 읽히며 눌러서 닫을 수 있다. 입력값 오류처럼 그 자리에서 고쳐야 하는 것은 예전처럼 입력칸 옆에 둔다.
- **스크롤 위치는 "떠나기 직전"에 적는다.** 화면이 바뀐 뒤에 읽으면 문서가 짧아지며 브라우저가 잘라 버린 값이 들어온다. 앱 안 이동은 `goTo()` 한 곳을 지나고, 뒤로·앞으로는 `popstate`(화면이 바뀌기 전에 온다)에서 적는다. 돌아온 직후에는 목록이 스켈레톤이라 그만큼 내려갈 수 없어서, 목록이 채워지면 한 번 더 맞추고 사용자가 직접 스크롤하면 그만둔다. 위치는 `location.key` 가 아니라 주소로 나눈다(이 라우터 설정에서는 key 가 계속 "default" 라 화면 전환을 알아채지 못한다).
- **PWA 는 플러그인 없이 직접 쓴다(2026-09-20).** `vite-plugin-pwa` 는 빌드 결과 전부를 미리 캐시하는 방식이라, 무엇이 캐시됐는지 알기 어렵고 이 앱에는 과하다. 캐시 규칙이 네 줄이면 끝난다: `/api/*` 는 **캐시하지 않고**(로그인한 사람마다 내용이 다르다), `/api/images/*` 와 `/assets/*` 는 캐시 우선(둘 다 주소가 고유해 내용이 바뀌지 않는다), 화면 이동은 네트워크 우선에 실패하면 캐시한 셸로 연다. 사진 캐시는 120장으로 제한해 저장소를 무한정 쓰지 않는다.
- **새 버전을 밀어 넣지 않는다(`skipWaiting`·`clients.claim` 없음).** 보고 있는 화면의 코드를 도중에 갈아치우면 화면과 코드가 어긋난다. 화면 이동이 네트워크 우선이라 새로고침하면 어차피 최신 코드가 온다. 새 버전이 설치되면 토스트로 알리고 적용 시점은 사용자가 정한다. 첫 설치는 "새 버전"이 아니므로 알리지 않는다(`serviceWorker.controller` 가 없으면 첫 설치다).
- **서비스 워커는 배포 화면에서만 켠다(`import.meta.env.PROD`).** 개발 서버에서 켜면 캐시 우선 규칙 때문에 고친 코드가 캐시에 갇혀 "왜 안 바뀌지"를 반복하게 된다.
- **앱 아이콘은 코드로 그린다(`scripts/make-icons.mjs`).** PNG 를 손으로 넣어 두면 색이나 모양을 바꿀 때 다시 만들 방법이 없다. 이미지 라이브러리 대신 Node 의 `zlib` 로 PNG 를 직접 쓴다(런타임 의존성을 늘리지 않는다는 규칙과 같은 이유다). `maskable` 아이콘은 운영체제가 어떤 모양으로 잘라도 마크가 살아남도록 가운데 80% 원 안에 들어오게 작게 그린다.
- **`vercel.json` 은 PWA 때문에 고치지 않는다.** `/((?!api/).*)` 규칙이 `/sw.js` 까지 index.html 로 보낼 것 같지만, Vercel 은 rewrites 보다 파일을 먼저 찾는다. 지금 `/assets/*.js` 가 정상 동작하는 것이 그 증거다.
- **선택한 동네는 `localStorage`에 둔다.** 세션과 달리 비로그인에도 유지돼야 하고, 민감한 값이 아니다.
- **토큰 재발급은 한 번만 보낸다.** 여러 요청이 동시에 만료되면 재발급 하나를 공유하고 나머지는 그 결과를 기다린다. 각자 재발급하면 서버가 rotation으로 이전 토큰을 폐기해 나머지가 로그아웃된다.
- **401 코드로 동작을 나눈다.** `EXPIRED_TOKEN`이면 재발급 후 재시도, `INVALID_TOKEN`·`UNAUTHORIZED`면 세션을 지운다. 네트워크 오류나 5xx에서는 토큰을 지우지 않는다.
- **찜하기·해제는 상품 상세에서 한다(2026-09-18).** 홈 목록 카드에서는 하트 버튼을 없애고 `♡ 3` 처럼 아이콘과 찜 수만 보여 준다. 목록에서는 카드 전체가 하나의 클릭 대상이 되고, 누를 수 있는 것과 그냥 표시인 것이 섞이지 않는다. 마이페이지 "찜한 상품" 탭만 예외로 카드에 해제 버튼을 남긴다(그 탭의 목적이 해제라서다). 상세에서 찜이 바뀌면 마이페이지를 다시 불러온다(찜한 상품 목록은 서버가 주는 것이라 화면에서 항목을 지울 수 없다).
- **모달 안의 "진행 중" 상태는 동작별로 나눈다(`ProductDetail.pending`: chat · favorite · manage).** 불리언 하나로 두면 찜을 누른 동안 채팅 버튼 글자가 "채팅방 여는 중…"으로 바뀌어 폭이 달라지고 옆 버튼이 밀린다(버튼이 튀는 현상). 버튼은 자기 동작 중일 때만 흐려지고, 요청이 겹치는 것은 `busy` ref 가 막는다. 숫자가 늘어도 폭이 변하지 않게 관심 수에는 `tabular-nums` 를 쓴다.
- **찜은 낙관적 갱신을 하지 않는다.** 서버가 준 `{ isLiked, favoriteCount }`만 반영한다. 하트와 관심 수가 어긋난 채 남는 것보다 잠깐 늦는 편이 낫다.
- **상품 상세 조회가 두 번 세어지면 안 된다.** 상세 조회가 조회수를 올린다. 라우터 도입 뒤에는 기록 항목별 요청 기억으로 지킨다(위 2026-09-17 라우터 결정 참고).
- **목록은 커서 페이징이다.** `nextCursor`를 해석하지 않고 그대로 돌려보낸다. 이어 붙일 때 id로 중복을 걸러낸다.
- **홈 목록은 무한 스크롤이다(2026-09-21, 사용자 요청).** "더 보기"를 누르는 것이 불편했다. 목록 끝의 1px 표시(`InfiniteTrigger`)가 화면 아래 600px 안에 들어오면 다음 페이지를 부른다. 스크롤 이벤트마다 위치를 재지 않고 `IntersectionObserver` 에 맡긴다. 불러오는 동안·실패했을 때는 표시를 빼서 요청이 겹치거나 실패 뒤 자동으로 쏟아지지 않게 한다(실패하면 "다시 시도"). 다 불러온 뒤에도 표시가 화면 가까이면 다시 붙으며 한 번 더 부르므로, 첫 페이지가 화면보다 짧아도 채워진다. 알림함·마이페이지 목록은 "더 보기" 그대로다.
- **상품 사진은 가로 한 줄 스크롤 스냅이다(2026-09-21).** 모바일은 손가락으로 넘기고(제스처 코드를 직접 쓰지 않아 관성·되돌아옴이 기기 기본 그대로다), PC 는 가로 스크롤을 막고 ← → 버튼으로 옮긴다. 몇 번째인지는 어느 쪽이든 스크롤 위치에서 계산한다. "1 / 2" 글자 대신 사진 수만큼 점을 두고 지금 사진의 점은 색과 길이로 구분한다. 점은 누를 수 없는 표시이고 보조기기에는 "N장 중 M번째 사진"을 읽어 준다. 사진은 처음부터 모두 받는다(`decoding="async"`, 최대 10장).
- **사진 줄에 `scroll-behavior: smooth` 를 두지 않는다(2026-09-21).** 줄 전체에 걸었더니 모바일에서 손을 뗀 뒤 스냅을 그 애니메이션이 가로채 뚝뚝 끊겼다(사용자 보고). 부드러운 이동은 PC 화살표 코드(`scrollTo({ behavior: "smooth" })`)에서만 한다. 넘기는 도중 사진을 받기 시작하던 `loading="lazy"` 도 뺐다. 몇 번째 사진인지는 한 프레임에 한 번(`requestAnimationFrame`)만 계산한다.
- **스크롤이 멈췄는데 사진 경계에 맞지 않으면 코드가 가장 가까운 사진으로 맞춘다(`settle`).** 실제 아이폰에서 앞 사진이 걸친 채 멈추는 캡처가 왔다. iOS 는 세로로 스크롤되는 창(모달) 안의 가로 스냅을 가끔 놓친다. 마지막 스크롤 150ms 뒤에 보고, 손가락이 닿아 있는 동안에는 건드리지 않는다.
- **상품 상세는 "누가 · 무엇을 · 얼마에" 순서이고, 사는 사람의 할 일은 아래 고정 바에 둔다(2026-09-21 UI 검토).** 채팅하기가 설명 맨 아래에 있어 설명이 길면 끝까지 내려야 보였다. 바에는 찜(하트) · 가격(제안 가능 여부) · 채팅하기만 둔다(당근마켓과 같은 배치). 모달 스크롤 영역 안의 `position: sticky` 이고, 모바일 전체 시트에서는 설명이 짧아도 화면 맨 아래에 붙게 `.content:has(> .buyBar)` 를 세로 flex 로 바꾼다(모달 공용 규칙을 건드리지 않는다). 판매자는 사진 바로 아래로 올렸고, 카테고리·동네·시간과 조회·채팅·관심을 각각 한 줄로 합쳤다(내 상품의 "관심 N" 이 판매자 사진 아래 혼자 떠 있던 것도 이걸로 사라졌다). "판매중" 태그는 기본 상태라 목록처럼 숨긴다.
- **내 상품 관리는 판매 상태 한 줄 선택 + 수정·끌어올리기 + 맨 아래 삭제 글자다.** 버튼 다섯 개가 줄바꿈되며 쌓였었다. 상태는 셋 중 하나라 설정의 화면 모드와 같은 세 칸 고르기(`.seg`, index.css 로 옮겨 함께 쓴다)로 바꿨다. 판매완료는 되돌릴 수 없어 확인을 받고, 판매완료가 되면 칸 전체가 잠긴다. 삭제는 드물고 되돌릴 수 없어 버튼 줄에서 뺐다.
- **상품 등록 폼은 사진이 맨 위다(2026-09-21).** 중고거래는 사진이 먼저다. 가격은 `type="number"` 가 아니라 글자 칸 + 숫자 키패드(`inputMode="numeric"`)이고 쉼표를 찍어 보인다(상태에는 숫자만, 10자리까지). 숫자 칸은 쉼표를 찍을 수 없어 1000000 의 자릿수가 안 보였다. 거래 방식은 둘뿐이라 목록 대신 두 칸 고르기(`.seg`)다. 숫자 거르기·쉼표 찍기는 `data/priceInput.js` 로 빼 테스트한다 — 처음 넣은 코드는 편집 스크립트가 정규식의 역슬래시를 먹어 `/D/g` 가 됐고, 쉼표가 남은 값이 `NaN` 으로 입력칸에 찍혀 지워지지도 않았다(사용자가 실제 기기에서 발견).
- **동네를 인증하지 않았으면 등록 창 안에서 바로 인증한다(2026-09-21).** 예전에는 "내 동네 인증하기"가 마이페이지로 옮기기만 해서, 다시 설정 → 내 동네를 찾아가야 했다(사용자 보고). 폼 대신 안내 카드(핀 아이콘 · 한 문단 · 버튼 하나)를 보이고, 인증이 끝나면 같은 창에서 폼이 열리며 방금 인증한 동네가 골라져 있다. 홈 동네도 대표 동네로 맞춘다(설정의 내 동네와 같은 `applyPrimaryRegion`).
- **읽은 알림은 글자를 연하게 한다(2026-09-21).** 안 읽은 알림의 배경·왼쪽 막대에 더해, 읽은 알림은 제목을 보통 굵기·연한 색으로 둔다.
- **요청은 화면이 바뀌면 `AbortController`로 취소한다.** 느린 이전 요청이 나중에 도착해 새 화면을 덮어쓰는 것을 막는다.
- **찜 버튼은 카드 버튼 바깥에 둔다.** `button` 안에 `button`은 유효하지 않은 HTML이라 브라우저마다 클릭 동작이 달라진다.
- **브레이크포인트는 880px 하나다.** 모바일 우선으로 쓰고 `@media (min-width: 880px)`에서 PC로 전환한다.
- **채팅 전송은 REST, 수신은 WebSocket 이다.** 백엔드가 그렇게 설계됐다(API-SPEC 10절). 보낸 사람 화면은 REST 응답으로 그리고, 같은 메시지가 소켓으로 다시 오면 `message.id`로 거른다.
- **소켓은 로그인한 동안만 연결한다.** App 이 `user.id` 기준으로 start/stop 한다. 연결할 때마다 현재 access token 을 CONNECT 헤더에 싣고, `EXPIRED_TOKEN` 거부는 `client.refreshTokens()`(REST 와 같은 재발급 비행)를 공유한 뒤 자동 재연결에 맡긴다. 소켓이 따로 재발급하면 동시 재발급으로 한쪽 토큰이 폐기된다.
- **소켓이 (재)연결될 때마다 목록·방을 REST 로 다시 불러온다.** 끊긴 동안의 이벤트는 서버가 다시 보내지 않는다. 방은 불러온 이전 메시지와 이어 붙이지 않고 최신 페이지로 바꾼다. 끊긴 동안 한 페이지보다 많이 오면 중간이 빈 채로 이어 붙기 때문이다.
- **읽음 처리는 화면이 보일 때만 보낸다.** 방에 들어올 때, 보고 있는 동안 상대 메시지가 올 때, 탭이 다시 보일 때(`visibilityState`). 백그라운드 탭에서 "읽음"이 뜨면 거짓 표시다. 요청 중에 온 메시지는 끝난 뒤 한 번 더 보낸다.
- **채팅 목록의 안 읽은 수는 서버 이벤트만 따른다.** 상대 메시지 이벤트에 +1, 내가 읽었다는 READ 이벤트에 0. 선택한 방인지 목록이 따로 판단하지 않는다.
- **채팅 입력의 Enter 는 한글 조합 중이면 무시한다**(`isComposing`, keyCode 229). 조합 중 Enter 로 보내면 마지막 글자가 빠지거나 두 번 전송된다. Shift+Enter 는 줄바꿈.
- **전송 버튼은 입력칸의 초점을 빼앗지 않는다(`mousedown` 기본 동작을 막는다).** 한 줄 보내고 이어서 쓰는 것이 보통인데, 초점이 버튼으로 옮겨가면 모바일 키보드가 닫힌다. 게다가 보낸 뒤에는 입력칸이 비어 이 버튼이 `disabled` 가 되므로 초점이 갈 곳조차 없어진다. 기본 동작만 막으면 `click` 과 폼 제출은 그대로 온다(브라우저로 양쪽을 확인했다: 막으면 초점이 입력칸에 남고, 막지 않으면 버튼으로 간다). 전송이 끝난 뒤에도 초점을 되돌리지만, 키보드를 내리고 누른 경우(`hadFocus` 가 거짓)에는 다시 올리지 않는다.
- **떠 있는 "상품 등록" 버튼은 홈에서만 띄운다(2026-09-21).** 채팅에서는 모바일 전송 버튼을 가리고, 나의 골목·설정에서는 쓸 일이 없다(사용자 요청). 버튼이 뜬 화면에서만 PC footer 아래에 버튼 자리(32 + 52 + 20px)를 비운다. 맨 아래까지 내리면 버튼이 footer 오른쪽 글자를 가렸다.
- **거래 버튼은 서버의 `tradeActions`만 보고 보여준다.** 역할·상태 조합을 프론트가 계산하지 않는다. 버튼 응답이 갱신된 방 정보라 그대로 반영하고, 상대 화면은 SYSTEM 메시지 이벤트를 받으면 방 정보만 다시 불러온다(메시지 목록은 다시 받지 않는다). 채팅 목록도 SYSTEM 메시지면 상품 상태 표시를 위해 다시 불러온다.
- **거래완료·예약 취소는 확인 창을 띄운다.** 거래완료는 되돌릴 수 없다. 예약하기는 취소할 수 있어 확인 없이 바로 보낸다.

## 디자인 규칙 (2026-09-18)

- **값은 `index.css` 토큰만 쓴다.** 간격 `--s1`~`--s9`(4px 배수), 글자 `--fs-caption`~`--fs-display`, 높이 `--control`/`--control-sm`, `--radius-*`, `--shadow-*`, 상태색 `--danger`/`--danger-bg`. 컴포넌트 CSS 에 새 px 값을 만들지 않는다.
- **버튼 모양은 `.btn` 한 곳에서만 정의한다.** 변형은 `btn-primary`/`btn-outline`/`btn-ghost`/`btn-danger`, 크기는 `btn-sm`/`btn-lg`/`btn-block`. 모듈 CSS 에서는 `composes: btn btn-primary from global;` 로 가져다 쓴다(`composes` 는 클래스 하나짜리 선택자에만 쓸 수 있다).
- **색은 예외 없이 토큰으로 쓴다.** 어두운 화면은 `index.css` 의 색 토큰만 다시 정의해서 만든다. 컴포넌트 CSS 에 `#fff`·`white` 같은 값이 하나라도 남아 있으면 그 자리만 밝은 채로 남는다. 강조색 위 글자는 `--on-accent`, 위험 버튼 글자는 `--on-danger`, 입력칸 바탕은 `--field`, 반투명 덮개는 `--overlay`·`--surface-blur`·`--surface-veil` 를 쓴다.
- **입력칸은 좁은 화면에서 16px 이하로 두지 않는다.** iOS 는 16px 보다 작은 입력칸을 터치하면 화면을 확대하고(끌 수 없다), 확대되면 위쪽이 화면 밖으로 밀려 무엇을 쓰는지 보이지 않는다. `index.css` 의 모바일 규칙이 `!important` 로 강제한다(컴포넌트마다 `font: inherit` 같은 더 강한 규칙이 있어서다).
- **화면 맨 아래에 붙는 것은 홈바 높이를 더한다.** 하단 탭·떠 있는 등록 버튼·목록 아래 여백·채팅방 높이 모두 `env(safe-area-inset-bottom)` 을 반영한다. `index.html` 의 `viewport-fit=cover` 가 있어야 이 값이 실제 크기로 계산된다(둘 중 하나만 있으면 무의미하다).
- **채팅방 높이는 재서 계산한다(눈대중 상수 금지).** `보이는 높이(--vvh) − 방이 시작하는 위치(--chat-top) − 하단 탭 공간(--nav-space) − 여유`. 예전에는 헤더·탭을 합쳐 190px 로 박아 뒀는데 헤더 높이가 기기 폭·글꼴 크기에 따라 달라져 어떤 기기에서는 입력창이 키보드에 가렸다. `--chat-top` 은 `ChatRoom` 이 `getBoundingClientRect` 로 재고(키보드가 밀어 올린 `offsetTop` 을 빼고), `--nav-space` 는 키보드가 열리면 0 이 된다.
- **키보드 높이는 `documentElement.clientHeight - visualViewport.height` 로 잰다.** `window.innerHeight` 를 쓰면 안 된다. iOS 크롬은 키보드가 올라오면 `innerHeight` 까지 같이 줄여서 차이가 0 이 된다(실제 아이폰 측정값: innerHeight 390 · 보이는 높이 390 · 레이아웃 높이 684 · 실제 키보드 294). 레이아웃 높이만 키보드와 무관하게 유지된다.
- **키보드가 올라오면 채팅방을 화면에 고정한다(`position: fixed; bottom: 0; height: var(--vvh)`).** 높이를 계산해 맞추는 방식은 기기마다 어긋났다. iOS 크롬에서 `bottom: 0` 으로 고정한 요소는 **키보드 위에** 그려지는 것을 실제 기기로 확인했다(진단 페이지). 보이는 높이만큼 채우면 헤더·대화·입력창이 정확히 한 화면에 들어온다.
- **대화 영역이 줄어들면 최신 대화로 다시 내린다.** 키보드가 올라와 방이 줄어들 때 브라우저는 `scrollTop` 을 그대로 둔다. 그래서 줄어든 높이만큼 아래쪽(최신)이 잘리고 위쪽 옛 대화가 보인다. `visualViewport` 의 resize 마다, 그리고 입력칸에 초점이 갈 때 맨 아래로 내린다. 위로 올려 옛 대화를 읽던 중(`stickToBottom` 이 거짓)이면 건드리지 않는다. 키보드가 여러 단계로 올라오므로 다음 프레임에 한 번 더 맞춘다.
- (지난 시도) `innerHeight - visualViewport.height` 와 방 높이 직접 계산은 iOS 에서 실패했다. 되돌리지 말 것. iOS 는 키보드를 띄우며 페이지를 밀어 올리는데(`offsetTop` 이 커진다), 그 값을 빼면 키보드 높이가 작게 계산돼 "열림"을 놓친다. 실제 아이폰에서 이 버그로 하단 탭이 그대로 떠 있었다.
- **모바일 키보드 높이는 `viewport.js` 가 CSS 변수로 알려 준다**(`--vvh`·`--kb`·`<html data-keyboard>`). iOS 는 키보드가 올라와도 레이아웃을 줄이지 않고 덮기만 해서 `100dvh` 로 짠 화면이 잘린다. 채팅방 높이는 `var(--vvh, 100dvh)` 기준이고, 키보드가 올라오면 하단 탭을 숨겨 그 높이(`--nav-reclaimed`)를 화면에 돌려준다.
- **브레이크포인트는 880px 하나.** 모바일 우선으로 쓰고 `@media (min-width: 880px)` 에서 PC 로 바꾼다.
- **로딩은 `Skeleton.jsx`, 빈 목록은 `EmptyState.jsx`, 오류는 전역 `.alert.alert-danger` 로 그린다.** 새 목록 화면을 만들면 이 세 가지를 그대로 쓴다.
- **파일 선택은 기본 `<input type="file">` 버튼을 그대로 보이지 않는다.** 브라우저마다 생김새가 다르고 화면과 따로 논다. 입력칸은 라벨 안에 투명하게 깔고(라벨을 누르면 열린다) 카메라 타일을 보여 준다. 상품 등록 폼은 사진 격자의 첫 칸이 타일이고 그 안에 `0/10` 이 있다. 프로필 사진도 같은 방식(라벨 + 숨긴 입력)이다.
- **초점 링은 키보드(Tab)로 옮겨 다닐 때만 그린다(2026-09-21, `inputMode.js`).** 창(dialog)을 열면 브라우저가 첫 버튼(닫기)에 초점을 옮기는데, iOS Safari 가 여기에 초점 링을 그려 아무것도 누르지 않았는데 닫기 버튼에 주황 동그라미가 쳐져 보였다(실제 아이폰 캡처). 모든 모달과 사진 크게 보기에 같은 경로다. `<html data-input>` 에 pointer/keyboard 를 적고, pointer 동안 버튼·링크·`[tabindex]` 의 링을 숨긴다(사진 고르는 칸의 `:focus-within` 링도). 입력칸은 대상이 아니다. Tab 만 키보드로 본다(휴대폰 화상 키보드도 keydown 을 보낸다). 초점을 닫기 버튼 대신 창 자체로 옮기는 방법도 있지만, 창을 닫고 돌아올 때의 초점 복원 등 다른 자동 초점까지 한 번에 막지 못해 입력 방식 기준으로 했다.
- **아이콘은 `Icon.jsx` 의 선 아이콘만 쓴다.** ⌂ ◎ ◌ 같은 글자 기호는 기기·글꼴마다 굵기와 크기가 달라 줄이 어긋난다. 같은 굵기(1.8)·같은 격자(24)로 그리고, 장식이라 항상 `aria-hidden` 이다. 뜻은 옆 글자나 `aria-label` 이 전한다.

## 알려진 과제

- (해결, 2026-09-22) 서버 삭제 지연 중 로그아웃되어 이전 계정 푸시가 남는 문제, 계정 전환·세션 만료 시 구독이 남는 문제, 서버 등록 없이 켜짐으로 보이는 문제를 수정했다. 기존 버전의 소유자 불명 구독은 첫 실행에서 해제되므로 설정에서 알림을 다시 켜야 한다.
- (해결, 2026-09-22) response.text 실패가 null 성공으로 처리되던 문제, 캐시 저장 실패가 정상 응답까지 실패시키던 문제, 알림의 역슬래시 경로로 외부 이동이 가능한 문제를 수정했다.
- (해결, 2026-09-22) 아이폰 브라우저 탭(홈 화면 앱 아님)에서 로그아웃이 실패하던 문제: `pushManager` 가 없을 때 TypeError 가 로그아웃을 막았다. 앱 시작 때의 계정 동기화도 같은 오류 토스트를 띄울 수 있었다.
- (해결, 2026-09-22) 푸시 수신과 계정 전환 후 미수신: **사용자가 실제 휴대폰(홈 화면 앱)에서 확인했다** — 알림 다시 켜기 → 다른 기기에서 보낸 채팅 알림 수신 → 로그아웃 뒤 미수신 → 다른 계정으로 로그인한 뒤 이전 계정 앞 채팅 미수신. 갤럭시에서 같은 흐름은 따로 확인하지 않았다.
- (이전 기록) 실제 아이폰·갤럭시의 푸시 수신 및 계정 전환 후 미수신은 사용자 확인 필요. 단위 테스트의 브라우저 객체 대역만으로 실기기 전송을 확인했다고 간주하지 않는다.


- (해결) 리뷰 작성·조회 화면이 없던 과제: 채팅방·구매내역의 후기 폼, 상대 프로필, 마이페이지 받은 후기 탭을 구현했다.
- (해결) 프로필 사진이 마이페이지·이웃 프로필에만 보이던 과제: 채팅방·채팅 목록·상품 상세 판매자·후기 작성자에도 표시한다. 상품 목록 카드·구매내역 카드에는 넣지 않았다(카드가 복잡해진다).
- 홈에서 PC 패널과 모바일 칩이 둘 다 마운트돼(하나는 CSS 로 숨김) 인기 검색어 요청이 두 번 나간다. 서버가 1분 동안 결과를 기억해 부담은 작다.
- Vite 개발 서버가 새로 만든 파일의 변환을 오래된 상태로 들고 있어 빈 화면(`does not provide an export named default`)이 난 적이 있다. 빌드는 정상이다. `node_modules/.vite` 를 지우고 개발 서버를 다시 띄우면 된다.
- 무한 스크롤이라 상품이 많은 동네에서는 PC footer 에 닿기 어렵다(다 불러와야 나타난다). 흔한 한계라 그대로 둔다.
- 알림 버튼은 헤더에만 있고 모바일 하단 메뉴에는 없다. 하단 메뉴는 2026-09-21 동네 선택을 빼 홈·채팅·나의 골목 세 칸이다(동네는 헤더의 동네 이름 버튼으로 바꾼다).
- 판매자가 채팅방을 나간 뒤 완료 거래로 다시 접근하는 별도 거래내역 화면은 없다. 현재 판매내역은 상품 기준이다.
- 상대가 후기를 남겨도 이미 열려 있는 화면에 실시간 반영하지 않는다. 화면을 다시 열거나 새로고침하면 반영된다.
- 실제 리뷰 E2E는 `review-*@golmok.test` 계정·거래·후기·사진을 남긴다. 해당 실행의 상품만 soft delete하며 사용자 실계정과 기존 데모 데이터는 건드리지 않는다.

- (해결) `vercel.json`의 백엔드 주소가 자리표시자이던 문제: 2026-09-18 실제 주소(`golmok-api.duckdns.org`)로 바꿨다. Vercel 의 rewrites 는 환경변수를 쓸 수 없어 파일에 직접 적는다. **백엔드 도메인이 바뀌면 이 파일도 함께 고쳐야 한다.**
- 채팅은 Vercel 을 거치지 못하므로 Vercel 환경변수에 `VITE_WS_URL=wss://golmok-api.duckdns.org/api/ws` 를 넣어야 한다. 빠뜨리면 다른 기능은 되는데 실시간 채팅만 연결되지 않는다.
- (해결) 상품 등록·수정·삭제·상태 변경·끌어올리기와 판매내역 화면이 없던 문제: 실제 API와 연결했다.
- (해결) 모바일 하단 메뉴가 버튼 4개를 3열로 배치해 등록 버튼을 가리던 문제: 4열로 수정하고 모바일 실제 클릭을 검증했다.
- (해결) 기존 모킹 E2E의 `**/api/**` 패턴이 `/src/api/*.js` 소스 요청까지 가로채 빈 화면을 만들던 문제: URL pathname이 `/api/`로 시작하는 요청만 대상으로 한정했다. 모킹 시나리오 추가 없이 기존 회귀 검증을 복구했다.
- (해결) 가입 오류 후 기존 E2E가 이메일 입력을 못 찾던 문제: label 안에 오류 문구가 붙어 정확한 이름이 달라지므로 고정된 input name으로 찾도록 변경했다.
- (해결) 상세의 regionId 누락과 공통 클라이언트의 JSON 전용 처리가 수정/업로드 연결을 막던 문제: 백엔드 상세 필드 추가와 FormData 지원으로 해결했다.
- 데모 상품의 외부 사진은 수정 시 삭제 후 다시 업로드해야 한다. 입력 화면에서 안내하며 외부 사진이 남으면 저장을 막는다.
- 실제 브라우저 검증용 `write-<실행식별자>@golmok.test` 계정·사진 파일은 남는다. 실행별 상품은 soft delete되며 사용자 실계정은 건드리지 않는다. 사진 정리 배치는 백엔드 후속 과제다.

- (해결) 라우터가 없어 새로고침하면 홈으로 돌아가고 링크 공유가 안 되던 과제: `react-router` 로 주소 기반 화면을 만들었다.
- (해결, 2026-09-22) 라우터 도입 뒤 기존 모킹 Playwright E2E 를 이 PC 에서 돌리지 못했다(브라우저 미설치): Playwright 전용 브라우저를 받지 않고 **이 PC 에 설치된 Chrome**(`channel: "chrome"`)으로 돌렸다. 설정 파일은 저장소 밖에 두지 않고 git 제외 폴더의 `.local-check/pw-chrome.config.js`(저장소 설정을 그대로 가져와 브라우저만 바꾼다)로 실행한다: `npx playwright test -c .local-check/pw-chrome.config.js`.
- (해결, 2026-09-22) 실제 서버 E2E(`product-write.spec.js`·`review.spec.js`)를 돌리지 못하던 과제: **사용자 로컬 DB 를 쓰지 않고** 일회용 Docker MySQL(127.0.0.1:3317, 임시 비밀번호) + 백엔드 jar(임시 JWT 비밀·임시 업로드 폴더, `bootRun` 이 아니라서 `~/.gradle` 의 로컬 비밀값이 끼어들지 않는다)로 띄워 돌렸다. 설정은 `.local-check/pw-live-chrome.config.js`(설치된 Chrome).
- 등록 폼의 설명·가격 칸은 라벨 안에 안내("10자 이상 …")·"원" 표시가 함께 있어 **접근 가능한 이름이 "설명 10자 이상…"·"가격 원" 처럼 길다**(E2E 가 정확한 이름으로 못 찾아 발견). 보조기기가 칸 이름으로 안내문까지 읽는다. 안내문을 라벨 밖으로 빼고 `aria-describedby` 로 잇는 것이 정석이다(후속 후보).
- (해결) 뒤로가기로 홈에 돌아오면 맨 위로 가던 문제: 주소별 스크롤 위치를 기억해 복원한다. 같은 주소의 서로 다른 기록 항목은 위치를 공유한다.
- 토스트에는 "되돌리기" 같은 동작 버튼이 없다. 실행 취소가 필요한 동작이 생기면 구조를 늘려야 한다.
- 로그인 상태의 채팅방·마이페이지 탭 주소 이동은 사용자 확인이 필요하다. 비로그인으로 직접 주소 진입·모달 체인·뒤로가기·검색 쿼리·잘못된 주소는 확인했다.
- (해결) **E2E가 API 모킹만 사용하던 과제:** 상품 쓰기·판매내역은 실제 MySQL 백엔드와 연결한 별도 E2E가 생겼다. 기존 모킹 E2E는 유지하며 찜 토글·구매확정·동네 인증 UI의 전체 실제 서버 회귀 검증은 후속 과제다.
- **채팅 화면을 로그인 상태의 실제 브라우저로는 확인하지 못했다(2026-09-17).** 실시간 흐름은 실제 서버·Vite 프록시로 프론트의 chatSocket 모듈을 돌려 확인했고, 화면은 비로그인·모바일 배치까지만 봤다. 채팅 E2E(Playwright `routeWebSocket` 모킹)도 아직 없다.
- (해결) 메뉴에 전체 안 읽은 메시지 뱃지가 없던 과제: 헤더 채팅 버튼·하단 탭에 합계 뱃지를 달았다(`GET /api/chat-rooms/unread-count`).
- 채팅 뱃지는 서버에 다시 묻는 방식이라 새 메시지 뒤 최대 300ms(+요청 시간) 늦게 바뀐다.
- 탈퇴 뒤 다른 기기는 access token 만료(최대 30분)나 재발급 실패 때 로그아웃된다. 그 전까지 일부 화면이 보일 수 있다.
- 소켓 연결 상태(끊김·재연결 중)를 화면에 표시하지 않는다.
- 차단당한 쪽 채팅 목록에서도 그 방이 사라진다(서버가 양방향으로 뺀다). 문구로는 드러나지 않지만, 방이 사라진 것으로 눈치챌 수는 있다.
- 내가 낸 신고 목록을 보는 화면은 없다(서버에도 조회 API 가 없다).
- 오프라인에서는 앱 화면만 열리고 목록·채팅 같은 데이터는 보이지 않는다(API 를 캐시하지 않기 때문이다). 의도한 동작이다.
- 주소창 색(`theme-color`)은 시스템 설정만 따른다. 앱 안에서 고른 화면 모드까지는 따라가지 않는다(메타 태그라 CSS 토큰이 닿지 않는다).
- 설치 버튼이 없다. 안드로이드는 브라우저 메뉴의 "앱 설치", iOS 는 공유 → "홈 화면에 추가" 로 설치한다. 안드로이드용 `beforeinstallprompt` 버튼은 후속 과제다.
- (해결) 푸시 알림이 없던 문제: 2026-09-21 웹 푸시를 붙였다. PC 에서 탭을 뒤로 숨겨 둔 사람은 WebSocket 이 살아 있어 푸시를 받지 못한다(탭의 뱃지로 본다).
- 서비스 워커의 캐시 동작은 `npm run build` 결과(`npm run preview`)에서만 확인할 수 있다. 개발 서버에서는 꺼져 있다.
- 배포 시 `VITE_WS_URL`(예: `wss://백엔드도메인/api/ws`)을 설정해야 한다. Vercel rewrites 는 WebSocket 을 중계하지 못한다.
- 어두운 화면에서 사진은 그대로 밝다. 목록에서 눈이 부시면 이미지에 약한 밝기 보정을 검토한다(지금은 사진 원본을 그대로 보여 준다).
- 스켈레톤은 상품·채팅·넓은 줄 세 가지뿐이다. 채팅방 말풍선처럼 형태가 다른 화면은 아직 글자 안내만 나온다.
- 채팅방·구매내역·상품 등록 폼 등 아직 자체 버튼 스타일을 가진 화면이 남아 있다. 모양은 토큰을 따르지만 `.btn` 으로 옮기지는 않았다.
- 접근성은 1차 점검만 했다(아래 진행 상황 참고). 실제 스크린리더(NVDA·VoiceOver)로 읽어 보는 확인은 아직이다.
- (해결) 목록에서 원본 이미지를 그대로 받던 문제: 목록 카드·채팅 목록·구매내역·프로필 사진은 서버 축소본(`/api/images/thumb/...`)을 쓴다. 상세의 큰 사진과 크게 보기는 원본이다. 데모의 외부 사진(picsum)은 그대로다.
- 사진 크게 보기에 확대·손가락 제스처는 없다. 좌우 이동과 닫기까지다.
- 백엔드가 `127.0.0.1`과 `localhost`를 모두 허용하지만, 배포 시에는 실제 도메인을 백엔드 `CORS_ALLOWED_ORIGINS`에 넣어야 한다. 빠지면 조회는 되는데 로그인만 403이 난다.

## 진행 상황

- [화면 확인] 2026-09-22 실제 빌드의 로컬 Vite preview + Edge(390×844)로 비로그인 /settings 화면, 로그인 안내 및 서비스 워커 등록을 확인했다. 페이지 JavaScript 오류 0. 목업 API·계정은 만들지 않았다. 로컬 백엔드는 실행하지 않아 /api/categories 연결은 실패했다. 따라서 API 연동, 로그인 후 푸시 버튼·실제 기기 수신은 미확인.


- [완료] 2026-09-22 검토 후 수정: 계정 전환·세션 만료·로그아웃과 푸시 구독 수명을 연결하고 구독 작업을 직렬화했다. 설정은 서버 재등록 성공 후에만 켜짐으로 표시하고 실패 시 재시도를 제공한다. 응답 본문 수신 실패를 성공으로 숨기지 않으며 서비스 워커는 캐시 실패와 네트워크 응답을 분리하고 알림 이동 경로를 제한한다.
- [검증] 2026-09-22 단위 테스트 103개 → 116개, 실패 0. npm run build 성공. 서버 삭제 지연·등록 실패·계정 전환 경쟁·세션 소멸, 응답 수신 실패, 캐시 용량 오류·저장소 오류, 외부 알림 경로 회귀 테스트 추가.


- [완료] 인증·상품 탐색·마이페이지·구매내역·내 동네 관리(인수 기준 `05ed546`).
- [완료] 상품 등록·수정 공용 폼, 실제 이미지 업로드·순서 변경·삭제, 본인 상세 관리, 판매내역·상태 필터 연결.
- [검증] 2026-09-16 단위 테스트 12개 → 14개, 실패 0. 백엔드는 256개 → 281개, 실패 0. 실제 MySQL 서버를 이용한 데스크톱·모바일 E2E 2개 통과: 등록·수정·사진 교체·등록 직후 끌어올리기 거부·상태 변경·탭 전환·삭제. 런타임 의존성 추가 없음. 커밋·푸시 미실행.
- [검증] 최종 `npm run build` 성공. 기존 모킹 E2E 10개와 별도 실제 서버 E2E 2개 모두 성공(총 12개, 실패 0). 실제 화면 캡처는 Git 제외 경로 `.local-check/product-write/`에 보관했다.
- [완료] 채팅: 상품 상세의 채팅하기, 채팅 목록(실시간 미리보기·안 읽은 수), 채팅방(실시간 수신·읽음 표시·이전 메시지·나가기), 헤더·하단 메뉴 진입.
- [검증] 2026-09-17 `npm run build` 성공. 단위 테스트 14개 → 20개(채팅 소켓 6개), 실패 0. 실제 MySQL 백엔드 + Vite 프록시로 데모 계정 두 개의 연결·CONNECT 인증·메시지 실시간 수신·보낸 사람 중복 수신·목록 안 읽은 수·읽음 이벤트·재연결을 확인했다. 기존 Playwright E2E 는 이 PC 에 브라우저가 설치돼 있지 않아 실행하지 못했다.
- [완료] 채팅방 직거래: 거래 줄(상태 + 서버가 준 tradeActions 버튼: 예약하기·거래완료·예약 취소), 시스템 메시지 가운데 표시, 상대가 바꾼 거래 상태를 시스템 메시지 수신 시 반영. 구매내역의 REQUESTED 표시를 "예약중"으로 변경.
- [검증] 2026-09-17 `npm run build` 성공, 단위 테스트 20개 실패 0. 실제 MySQL 백엔드 + Vite 프록시로 직거래 전 흐름과 시스템 메시지 실시간 수신 확인. 로그인 상태의 거래 줄 화면은 사용자 확인 필요.
- [완료] 리뷰·매너온도: 채팅방·구매내역에서 후기 작성, 상대 프로필·받은 후기 조회, 마이페이지 받은 후기 탭. 점수 선택·500자 제한·등록 중 중복 방지·작성 완료 후 재조회.
- [검증] 2026-09-17 인수 기준 `c8b9f7d`. `npm run build` 성공, 단위 테스트 20개 → 23개, 실패 0. 실제 MySQL 서버의 데스크톱·모바일 E2E 4개 통과(기존 상품 쓰기 2개 + 리뷰 2개). 후기 E2E는 판매자 채팅 작성·구매자 구매내역 작성·프로필 온도/본문·받은 후기 탭·중복 409·동시 요청 201/201/409 및 온도 합산을 확인했다. 최종 화면 캡처도 확인했다. 신규 런타임 의존성 없음.
- [완료] 알림: 헤더 알림 버튼(안 읽은 수 뱃지), 알림함 모달(최신순·더 보기·모두 읽음·열린 동안 실시간 추가), 알림을 누르면 상품 상세·채팅방·받은 후기 탭으로 이동.
- [검증] 2026-09-17 `npm run build` 성공, 단위 테스트 23개 → 27개(알림 경로 해석·조작된 경로 거부·API 호출 4개), 실패 0. 실제 MySQL 백엔드 + Vite 프록시로 알림 실시간 수신·안 읽은 수·읽음 확인. 로그인 상태 화면은 사용자 확인 필요.
- [제외] 결제(2026-09-17). 택배 거래 전용이라 범위에서 뺐다. 택배거래 상품도 채팅방 직거래 흐름으로 진행된다.
- [완료] 라우터: 주소 기반 화면(홈 쿼리·상세/프로필 모달·채팅방·마이페이지 탭·NotFound), 알림 경로 이동, Vercel SPA 대체 경로.
- [검증] 2026-09-17 `npm run build` 성공, 단위 테스트 27개 → 30개(주소 규칙 5개 추가, 알림 경로 해석 2개는 routes 로 이동), 실패 0. 실제 서버 + 브라우저(비로그인): `/products/43` 직접 진입 시 홈 위 모달·StrictMode 에서도 상세 요청 1회, 상세 → 프로필 → 뒤로가기 시 상세 복원·추가 요청 없음, 직접 진입 모달 닫기 시 홈으로 바꿔치기, `/?q=식탁&sort=PRICE_ASC` 새로고침 복원, 모달 닫은 뒤 검색 결과 유지, 새 검색 후 뒤로가기로 이전 검색 복귀, `/chat-rooms/abc`·`/my/admin`·`/nope` NotFound. 로그인 상태 화면은 사용자 확인 필요.
- [완료] 프로필 수정: 마이페이지 "프로필 수정" 창(닉네임·사진 즉시 업로드·사진 삭제), 저장 후 헤더 닉네임 즉시 반영, 마이페이지·이웃 프로필에 사진 표시.
- [검증] 2026-09-17 `npm run build` 성공, 단위 테스트 30개 → 31개(세션 사용자 갱신·다른 계정 늦은 갱신 무시), 실패 0. 실제 MySQL 백엔드로 변경·중복·길이·외부 사진 거부 확인. 로그인 상태 화면은 사용자 확인 필요.
- [완료] 프로필 사진 표시 확대: 공용 Avatar, 채팅방·채팅 목록·상품 상세 판매자·후기 작성자·마이페이지·이웃 프로필·프로필 수정.
- [검증] 2026-09-17 `npm run build` 성공, 단위 테스트 31개 실패 0. 비로그인 브라우저로 상품 상세 판매자 사진(44px)·이웃 프로필 사진(64px)·후기 작성자 사진(32px) 표시 확인. 로그인 상태의 채팅 화면은 사용자 확인 필요.
- [완료] 인기 검색어: PC 안내 패널 순위 목록(로딩·에러 다시 시도·빈 상태), 모바일 홈 가로 칩(검색 중·빈 결과·오류 시 숨김), 누르면 `/?q=` 검색.
- [검증] 2026-09-17 `npm run build` 성공, 단위 테스트 31개 실패 0. 실제 서버 + 브라우저(비로그인): PC 패널 순위 표시·클릭 시 `/?q=식탁` 이동, 375px 칩 표시·가로 넘침 없음, 검색 중 칩 숨김.
- [완료] 회원 탈퇴: 마이페이지 맨 아래 "회원 탈퇴" 링크 → 탈퇴 창(사라지는 것 안내·비밀번호·동의 체크, 비밀번호 오류는 입력칸 옆·진행 중 거래 등은 서버 문구). 성공하면 홈으로 옮기고 세션 삭제. 채팅방에서 상대가 탈퇴했으면 안내 문구·입력창/전송 비활성·프로필 버튼 비활성.
- [완료] 채팅 안 읽은 메시지 합계 뱃지: 헤더 채팅 버튼·모바일 하단 채팅 탭. 로그인·소켓 재연결·상대 새 메시지·내 읽음·페이지 이동 때 다시 불러오고(300ms 로 묶음), 99 초과는 99+.
- [검증] 2026-09-17 `npm run build` 성공, 단위 테스트 31개 → 33개(탈퇴 요청 형식·합계 API), 실패 0. 실제 MySQL 백엔드 + Vite 프록시로 탈퇴·합계 API 흐름 확인(`codex-handoff/scripts/withdrawal-live-check.mjs`). 로그인 상태 화면(탈퇴 창·뱃지·탈퇴 상대 채팅방)은 사용자 확인 필요.
- [완료] UI 정리 1·2단계(2026-09-18). 지금의 따뜻한 톤은 유지하고 구조만 정돈했다.
  - 토큰: 간격·글자·높이·그림자·상태색 스케일 추가, 공용 `.btn` 도입 후 모달·폼·마이페이지 버튼을 `composes` 로 연결.
  - 헤더: 모바일에서 채팅·나의 골목·로그인 숨김(하단 탭과 중복이었고 헤더가 두 줄이 됐다), 검색은 아래 줄 전체 폭, 로그인만 채운 버튼으로 강조.
  - 홈: 제목 40px → 28px(PC)·20px(모바일)로 줄이고 목록 머리말과 정렬을 한 줄에 배치, 구분선 추가.
  - 카드: 줄무늬 플레이스홀더를 조용한 면으로, 판매중 뱃지는 생략하고 예약중·판매완료만 표시, 값 → 동네 → 관심 수 순으로 크기 차등.
  - 사이드바: 강조 카드(동네 고르기) → 기본 카드(인기 검색어) → 테두리 없는 안내로 무게 차등.
  - 떠 있는 등록 버튼: 모바일은 52px 원형(카드 두 장을 가렸다), PC 는 글자 버튼 유지.
  - 하단 탭: 현재 탭에 색 외에 표시선 추가, 포커스 링 3px → 2px.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 33개 실패 0. 실제 서버 + 브라우저(비로그인)로 1280px·375px 홈, 상품 상세 모달, 동네 선택 모달, 마이페이지 비로그인 화면을 확인했다. 로그인 상태 화면(마이페이지·채팅·거래 줄)은 사용자 확인 필요. CSS 번들 39.3KB → 37.7KB.
- [완료] UI 정리 3단계(2026-09-18): 로딩·빈 상태·오류 표시 통일.
  - `Skeleton.jsx`: 상품 목록(모바일 가로형·PC 3열)·채팅 목록·넓은 줄 세 가지. 실제 카드와 같은 크기라 결과가 도착해도 화면이 튀지 않는다. 흐르는 빛은 `prefers-reduced-motion` 에서 멈춘다. 보조기기에는 목록 전체를 status 하나로 알린다.
  - `EmptyState.jsx`: 아이콘 + 제목 + 설명 + (필요할 때만) 버튼. 홈(동네 미선택·검색 결과 없음)·찜·판매내역·구매내역·채팅 목록·받은 후기에 적용. 검색 결과 없음은 검색어를 제목에 넣는다.
  - 오류 줄을 전역 `.alert.alert-danger` 로 모으고, 화면마다 있던 `#a12c1e`·`#fff0eb` 직접 지정과 자체 "다시 시도" 버튼 스타일을 없앴다.
  - 채팅 목록·구매내역 썸네일의 줄무늬 플레이스홀더도 조용한 면으로 바꿨다(상품 카드와 동일).
  - 동네를 안 고른 홈에서 부제와 빈 상태가 같은 문장을 두 번 말하던 것을 부제 쪽에서 없앴다.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 33개 실패 0. 브라우저(비로그인)로 홈 스켈레톤 → 결과 전환, 검색 결과 없음, 동네 미선택 빈 상태를 1280px·375px 에서 확인했다. 로그인 상태 화면(마이페이지 탭·채팅 목록·받은 후기)의 스켈레톤·빈 상태는 사용자 확인 필요.
- [완료] 찜 진입점 이동(2026-09-18): 홈 카드의 하트 토글 버튼 제거 → 하트 아이콘 + 찜 수 표시, 상품 상세에 "♡ 관심 N" 토글 버튼 추가(눌린 상태는 색 + 채운 하트 + `aria-pressed`). 상세 본문의 관심 수 중복 문구 제거, 내 상품은 찜할 수 없으므로 관심 수만 표시. 마이페이지 찜한 상품 탭의 해제 버튼은 유지.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 33개 실패 0. 브라우저(비로그인)로 홈 카드에 버튼이 없고 `♡ 3` 표시, 상세의 찜 버튼 배치, 비로그인 클릭 시 로그인 창 열림을 확인했다. 로그인 상태의 실제 찜·해제와 찜한 상품 탭 갱신은 사용자 확인 필요.
- [완료] 아이콘 정리와 하단 메뉴 축소(2026-09-18): `Icon.jsx`(집·핀·말풍선·사람·하트) 도입. 상품 카드의 "채팅 0" 글자를 말풍선 아이콘 + 숫자로, 찜 표시도 같은 집합의 하트로 바꿨다. 모바일 하단 메뉴는 글자 기호를 아이콘으로 바꾸고 로그아웃 탭을 없애 4칸이 됐다. 헤더 로그아웃은 이미 모바일에서 숨기고 있어, 로그아웃을 마이페이지 맨 아래(회원 탈퇴 왼쪽)로 옮겼다. 비로그인 상태의 로그인 진입점도 마이페이지가 맡는다.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 33개 실패 0. 브라우저(비로그인)로 375px 하단 4탭 아이콘·카드 아이콘 표시, 1100px 홈을 확인했다. 로그인 상태의 마이페이지 로그아웃 버튼은 사용자 확인 필요.
- [완료] UI 정리 4단계 — 어두운 화면(2026-09-18): 컴포넌트 CSS 에 남아 있던 색 61곳을 토큰으로 바꾸고(`--on-accent`·`--on-danger`·`--field`·`--overlay`·`--surface-blur`·`--surface-veil`·`--shimmer`·`--success-bg` 추가), `index.css` 에 따뜻한 회색 기반 다크 팔레트를 넣었다. 시스템 설정을 기본으로 따르고 헤더의 해·달 버튼으로 바꿀 수 있다(`theme.js`, localStorage 저장, 선택이 시스템보다 우선). `index.html` 인라인 스크립트로 첫 그림 깜빡임을 막는다.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 33개 → 40개(테마 7개: 시스템 추종·저장값 우선·토글·해제·이상한 값 무시·시스템 변경 알림·저장소 차단), 실패 0. 브라우저에서 시스템 다크로 홈(스켈레톤·목록·사이드바)·동네 선택 모달·상품 상세 모달을 확인하고, 토글로 밝은 화면이 시스템보다 우선하는 것까지 확인했다. 로그인 화면(마이페이지·채팅방·구매내역 뱃지)의 어두운 화면은 사용자 확인 필요.
- [완료] UX 개선(2026-09-18): 로그인 상태 유지 체크박스, 공용 토스트, 뒤로가기 스크롤 복원.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 40개 → 50개(토스트 6개, 세션 저장 위치 4개), 실패 0. 브라우저로 로그인 창의 체크박스, 토스트 모양(성공·기본·실패), 홈 1100px 스크롤 → 마이페이지(맨 위) → 뒤로가기(1100px 복원), 상세 모달 여닫기 시 위치 유지를 확인했다. 토스트가 실제로 뜨는 동작(찜 실패·프로필 저장·끌어올리기 등)은 로그인 화면이라 사용자 확인 필요.
- [완료] 사진 크게 보기(`Lightbox.jsx`): 상세의 사진을 누르면 화면 전체로 보고 좌우 키·버튼으로 넘긴다. 상세 모달 위에 dialog 를 하나 더 띄워 ESC·초점 가둠을 브라우저에 맡긴다.
- [완료] 모바일 모달을 전체 시트로: 880px 미만에서 모달이 화면을 가득 채우고 제목 줄이 위에 붙어 닫기 버튼이 늘 보인다(안전 영역 반영).
- [완료] 목록 사진에 서버 축소본 적용(`api/imageUrl.js`): 우리 서버 사진만 `thumb/` 주소로 바꾸고 외부 사진은 그대로 둔다.
- [완료] 접근성 1차 점검: 본문 바로가기 링크 추가(Tab 한 번에 나타남), 페이지마다 `<main id="main">`, 카테고리 줄 `role="group"`, 카드 제목 h3 → h2(단계 건너뜀 해소). 점검 결과 이름 없는 버튼·alt 없는 이미지·라벨 없는 입력·중복 id 는 0 이었다.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 50개 → 53개(축소본 주소 3개), 실패 0. 브라우저로 375px 전체 시트, 크게 보기(1/3 표시·이전 비활성), Tab 한 번에 본문 바로가기 노출, 접근성 자동 점검(이름 없는 버튼 0·alt 없는 이미지 0·라벨 없는 입력 0·중복 id 0·landmark 확인)을 확인했다.
- [완료] 상품 사진 정렬 개선(2026-09-18): 사진을 가로 한 줄로 늘어놓고 끌어서 순서를 바꾼다(맨 앞이 대표, "대표" 표시). ← → 버튼은 없앴고 키보드는 방향키로 옮긴다. 삭제는 사진 모서리의 × 버튼이다.
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 53개 → 58개(순서 계산 5개), 실패 0. 끌기 동작 자체(마우스·손가락)는 로그인해야 열리는 화면이라 사용자 확인 필요.
- [완료] **Vercel 배포(2026-09-18).** https://golmok-market-frontend.vercel.app · `vercel.json` rewrites 로 `/api/*` 를 백엔드(golmok-api.duckdns.org)에 전달하고, Vercel 환경변수 `VITE_WS_URL=wss://golmok-api.duckdns.org/api/ws` 로 채팅 소켓을 백엔드에 직접 연결한다(`VITE_API_BASE` 는 비워 둔다 — 값을 넣으면 rewrites 를 우회한다).
- [검증] 2026-09-18 외부 브라우저(비로그인)로 동네 선택 → 상품 20개 목록·사진·상태 뱃지 확인. 사용자가 로그인·채팅·**휴대폰 접속**까지 확인해 "기능이 모두 작동"이라고 확인했다. Vercel 미리보기 주소(`...-git-...vercel.app`)는 백엔드 CORS 목록에 없어 로그인이 막힌다(정식 주소로 테스트할 것).
- [완료] 모바일(iOS) 문제 수정(2026-09-18): ① 입력칸을 터치하면 화면이 확대되고 위가 잘리던 문제 — 좁은 화면 입력칸을 16px 로. ② 키보드가 올라오면 채팅방 상단이 잘리던 문제 — `viewport.js` 로 실제 보이는 높이를 받아 방 높이를 맞추고 하단 탭을 숨긴다. ③ `index.html` viewport 에 `viewport-fit=cover` 가 없어 안전영역(노치·홈바) 여백이 **전부 0 이던 것**을 고쳤다. ④ 현재 위치 찾기 실패 안내를 사유별로 나눴다(권한 거부·시간 초과·기타, 시간 초과 10 → 15초).
- [검증] 2026-09-18 `npm run build` 성공, 단위 테스트 58개 → 65개(뷰포트 7개), 실패 0. 375px 에서 입력칸 16px·PC 15px 유지 확인, 키보드 상태를 흉내 내 하단 탭이 사라지고 58px 를 돌려받는 것까지 확인했다. **실제 iOS 기기 확인은 사용자 몫이다**(이 PC 에 iOS 가 없다).
- [완료] iOS 채팅 화면 수정(2026-09-20): 키보드 감지를 `clientHeight` 기준으로 고치고, 키보드가 올라오면 채팅방을 화면에 고정한다. 입력칸을 누르거나 키보드로 높이가 바뀌면 최신 대화로 다시 내린다. 실제 아이폰에서 측정한 값(innerHeight 가 함께 줄어듦 · fixed 요소는 키보드 위에 표시됨)을 근거로 접근을 바꿨다.
- [검증] 2026-09-20 단위 테스트 65개 → 66개(iOS 실측값 재현), 빌드 성공. **사용자가 실제 아이폰(iOS 크롬)에서 채팅 입력이 키보드에 가리지 않는 것을 확인했다.** 원인 파악에 쓴 임시 진단 페이지(`public/viewport-check.html`)는 확인 뒤 지웠다. 측정값은 위 디자인 규칙에 남겼다.
- [완료] PWA(2026-09-20): 홈 화면에 설치해 주소창 없이 앱처럼 쓴다. manifest(이름·색·아이콘·바로가기), 직접 쓴 서비스 워커(사진·번들 캐시, API 캐시 금지, 오프라인 셸), 코드로 그린 아이콘 4종, 새 버전 알림 토스트.
- [검증] 2026-09-20 단위 테스트 66개 → 73개(등록 7개), 빌드 성공. 빌드 결과를 `npm run preview` 로 띄워 브라우저에서 확인했다: 서비스 워커 등록·활성, manifest 가 `application/manifest+json` 으로 응답, 새로고침 뒤 번들이 `golmok-assets-v1` 에 담김, API 응답은 캐시되지 않음. **서버를 멈춘 뒤 `/products/12` 로 들어가 앱이 캐시로 열리는 것까지 확인했다**(데이터만 오류). 배포 주소에서도 `/sw.js` 가 `application/javascript` 로 내려오고(rewrite 에 삼켜지지 않는다) 서비스 워커가 활성인 것을 확인했다. **사용자가 실제 휴대폰에 설치해 아이콘까지 확인했다.**
- [완료] 메시지를 보내도 키보드가 닫히지 않는다(2026-09-20): 전송 버튼이 입력칸의 초점을 빼앗지 않게 했다.
- [검증] 2026-09-20 단위 테스트 73개, 빌드 성공. 같은 구조의 페이지로 브라우저에서 양쪽을 확인했다(막으면 제출되고 초점이 입력칸에 남고, 막지 않으면 초점이 버튼으로 간다). **사용자가 실제 아이폰에서 연달아 보내며 확인했다.**
- [완료] 상품 등록 오류 안내 개선(2026-09-20): 설명 입력칸에 "10자 이상" 안내를 두고, 보내기 전에 다듬은 값으로 검사하며, 서버 필드 오류를 입력칸 아래에 한국어로 붙인다.
- [검증] 2026-09-20 빌드 성공, 단위 테스트 73개. 브라우저에서 확인: 공백을 섞은 10자는 브라우저 기본 검사를 통과하지만(`checkValidity()` true) 앱이 막아 요청을 보내지 않고(요청 0회) 설명칸 아래에 문구가 뜬다. 서버가 준 필드 오류도 같은 자리에 붙고 `aria-describedby` 로 연결된다. 서버 데이터를 건드리지 않으려고 응답은 브라우저 안에서 흉내 냈다. **사용자가 실제 기기에서 안내 문구가 뜨는 것을 확인했다.**
- [완료] 신고·차단 화면(2026-09-21): 신고 창(사유 5개·기타는 설명 필수·완료 화면에서 바로 차단), 상품 상세 "이 게시글 신고하기", 프로필 신고·차단/해제(서버의 `blockedByMe` 로 시작), 채팅방 점 세 개 메뉴(신고·차단, 차단하면 목록으로), 마이페이지 "차단한 사용자" 창(해제·더 보기·빈 상태).
- [검증] 2026-09-21 빌드 성공, 단위 테스트 73개 → 81개(신고·차단 API 5개, 빈 본문 응답 3개). 새 API 가 아직 배포 전이라 새 API 응답만 브라우저 안에서 흉내 내고 나머지는 배포 서버로 375px 에서 눌러 확인했다: 기타 사유는 설명이 없거나 공백뿐이면 신고 버튼이 꺼짐, 보낸 설명은 앞뒤 공백 제거, 완료 화면의 차단(본문 없는 201)이 오류 없이 "차단했어요", 프로필 차단 ↔ 해제 전환과 안내 문구, 채팅방 메뉴(바깥 누르기·Esc 로 닫힘, 차단하면 `/chat` 으로 이동), 마이페이지 계정 줄(로그아웃·차단한 사용자·회원 탈퇴)과 차단 해제 후 빈 상태. 신고 창·채팅 헤더·메뉴 모두 375px 안에 들어오고 가로 스크롤 없음. 백엔드 배포 뒤 **사용자가 실제 기기에서 신고·차단 네 진입점을 모두 확인했다.**
- [완료] 웹 푸시 화면(2026-09-21): 마이페이지 "휴대폰 알림" 줄(지원 안 함·홈 화면 설치 필요·권한 차단·켜기/끄기), 서비스 워커의 push·notificationclick, 로그아웃할 때 이 기기 구독 해제.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 81개 → 94개(지원 판별 4 · 키 변환 1 · 켜기 5 · 끄기 3). 빌드 결과를 띄워 브라우저에서 확인: 서비스 워커 등록, 알림 줄이 프로필 바로 아래 뜸, 권한 미정이면 "켜기", 거부하면 기기 설정 안내로 바뀌고 서버 요청을 보내지 않음(공개키 응답만 흉내 냈다). **실제 구독·알림 수신은 배포 뒤 실제 기기로 확인해야 한다**(이 브라우저 창은 푸시 서비스가 없다).
- [완료] 설정 화면(2026-09-21): 마이페이지 프로필 옆 톱니바퀴 → `/settings`(휴대폰 알림 · 내 동네 · 차단한 사용자 · 로그아웃 · 회원 탈퇴). 마이페이지는 프로필과 탭 목록만 남았다.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개. 빌드 결과를 375px 로 띄워 확인: 마이페이지 구성이 프로필·탭·목록 셋뿐이고 설정 항목이 남지 않음, 톱니바퀴가 화면 안(오른쪽 끝 359px), 설정 순서·하단 탭 "나의 골목" 켜짐·줄 높이 52px·탈퇴 위험 색·가로 넘침 없음, 차단한 사용자 창, 뒤로(앱 안 → 기록 되돌림 / 주소로 진입 → 기록 수 그대로 `/my`), 로그아웃 → 세션 삭제 후 `/my` 로그인 안내. 처음 로그아웃 확인에서 이동이 안 되는 것처럼 보였는데, **테스트가 모바일에서 숨겨진 헤더의 로그아웃 버튼을 눌렀던 것**이었다(같은 글자의 버튼이 DOM 에 둘 있다). 설정 안의 버튼을 정확히 골라 다시 확인했다.
- [완료] 상품 등록 버튼 정리(2026-09-21): 홈에서만 띄우고, PC footer 가 버튼에 가리지 않게 했다.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개. 1280px 로 맨 아래까지 내려 좌표로 확인: 홈 footer 글자 681~696px · 버튼 716~768px(겹침 없음), 나의 골목·설정은 버튼 없음·footer 원래 높이(80px).
- [완료] 화면 모드를 설정으로, 헤더 로그아웃 제거(2026-09-21): 헤더의 해·달 버튼과 로그아웃 버튼을 없애고, 설정에 "화면 모드"(시스템·밝게·어둡게)를 휴대폰 알림 바로 아래에 뒀다. 비로그인 나의 골목에는 "로그인하기" 옆에 "설정" 버튼을 두고, 비로그인 설정 화면은 화면 모드와 로그인 안내만 보인다.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개. 빌드 결과를 띄워 확인: 375px·1280px 헤더에 해·달·로그아웃 버튼이 없음(비로그인 PC 는 채팅·나의 골목·로그인), 설정에서 어둡게 → `data-theme=dark`·저장값 dark·배경 어두움, 밝게 → light, 시스템 → 속성·저장값 삭제 후 "기기 설정을 따라요" 문구, 고른 칸만 `aria-pressed=true`, 375px 가로 넘침 없음, 비로그인 나의 골목의 두 버튼이 한 줄에 같은 높이로 놓이고 설정 → `/settings` 에 화면 모드와 로그인 안내. 로그인 상태 화면은 서버 없이 띄워 내 동네 부분이 오류로 나왔다(화면 모드 자체는 확인). 이 검증 중 미리보기 브라우저에 남아 있던 예전 로컬 로그인 세션을 지웠다(실제 기기와 무관).
- [완료] 읽은 알림 연하게, 홈 무한 스크롤, 상품 사진 넘기기·점 표시(2026-09-21).
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개. 빌드 결과를 운영 API 에 붙여 확인: 홈(역삼동)에 "더 보기" 버튼 없음, 끝 근처에서 커서 요청이 **한 번만** 나가 20 → 24개로 늘고 다음 페이지가 없으면 표시가 사라짐. 상세(사진 2장) 375px: 화살표 숨김·가로 스크롤 가능·스냅, 옆으로 넘기면 두 번째 점이 켜지고 "2장 중 2번째 사진", 가로 넘침 없음. 1280px: 화살표 보임·가로 스크롤 막힘, 다음을 누르면 한 칸(495px) 이동하고 점이 바뀜. **한계:** 확인하는 동안 브라우저 창이 가려진 상태라 `IntersectionObserver` 가 실제로 울리지 않아, 같은 판정을 하는 가짜 관찰자로 바꿔 흐름을 확인했다. 손가락 넘기기의 느낌과 읽은 알림 모양(로그인 필요)은 사용자 확인이 필요하다. 처음 수정에서 `export default` 와 `function App` 사이에 새 컴포넌트가 끼어 화면이 빈 채로 뜬 것을 브라우저에서 발견해 고쳤다. 모바일 화살표 숨김은 전역 `.btn` 의 `display` 에 밀려 처음에 적용되지 않았다(한 단계 구체적인 선택자로 해결).
- [완료] 사진 넘기기 끊김·어긋남 수정(2026-09-21): CSS 부드러운 스크롤 제거, 사진 미리 받기, 멈춘 뒤 경계 맞추기.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개. 브라우저 375px: 줄의 `scroll-behavior` 가 auto, 두 사진이 열자마자 모두 받아짐, 70% 에서 멈추면 두 번째 사진(343px)에 맞춰짐. **한계:** 데스크톱 Chrome 은 코드로 어긋나게 옮겨도 스스로 바로 스냅해 iOS 의 어긋남을 재현할 수 없었다 — 경계 맞추기가 실제로 필요한 순간은 아이폰에서만 확인된다. 점 갱신은 창이 가려져 프레임이 돌지 않아 여기서 보지 못했다(앞 검증에서는 확인). **사용자가 아이폰에서 확인해야 한다.**
- [완료] UI 검토 반영(2026-09-21): ① 상품 상세 하단 고정 바(찜·가격·채팅하기) ② 상세 정보 재배치(판매자 위로, 메타 두 줄, 판매중 태그 숨김) ③ 내 상품 관리 정리(상태 세 칸 선택, 삭제는 아래 글자) ④ 하단 탭 세 칸(동네 선택 제거) ⑤ 홈 "불러온 상품 N개" 제거(검색 중에만 "검색 결과"), 카드의 0 인 관심·채팅 수 숨김. 실제 서버 E2E(`tests/product-write.spec.js`)의 상태 변경·삭제 버튼 선택자도 새 화면에 맞췄다.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개. 빌드 결과를 운영 API 에 붙여 확인(비로그인): 375px 하단 탭 홈·채팅·나의 골목, 홈 제목 아래 개수 문구 없음, 카드 숫자(관심 3 채팅 1 / 관심 3 / 채팅 1 / 0 이면 줄 없음), 상세 순서 사진 → 판매자 → 제목 → 메타 → 태그(직거래) → 설명 → 조회·채팅·관심 → 신고 → 바, 바가 화면 맨 아래 전체 폭(747~812px)이고 설명을 길게 늘려도 맨 위·중간·끝에서 같은 자리, 가로 넘침 없음. 1280px: 바가 모달 아래에 붙고 긴 설명에서도 유지, 화살표 보임. **내 상품 관리 화면(상태 세 칸·삭제 글자)은 로그인이 필요해 보지 못했다 — 사용자 확인 필요.** 수정한 E2E 도 이 PC 에서 돌리지 못했다(로컬 백엔드 필요). 확인하며 1번 상품 상세를 세 번 열어 조회수가 3 올랐다.
- [완료] 상품 등록 폼 정리(2026-09-21): 사진 맨 위, 가격 쉼표 표시·숫자 키패드·"원" 표시, 거래 방식 두 칸 고르기. E2E 의 가격 라벨("가격 (원)" → "가격")도 맞췄다.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개. **등록 폼은 로그인해야 열려 브라우저로 보지 못했다 — 사용자 확인 필요**(가격 입력 중 쉼표가 찍힐 때 커서 위치 포함). 수정한 E2E 도 로컬 백엔드가 없어 돌리지 못했다.
- [완료] 가격 입력 NaN 수정, 등록 창 안 동네 인증(2026-09-21).
- [검증] 2026-09-21 빌드 성공, 단위 테스트 94개 → 99개(가격 입력 5개: 쉼표 섞인 입력·지우기·숫자 외 글자·10자리·서버 숫자 표시). 브라우저 375px 에서 로그인·API·위치를 흉내 내 확인(서버 요청 없음): 동네 없는 계정으로 등록을 누르면 안내 카드(64px 핀 · 전체 폭 버튼), 버튼을 누르면 `POST /api/users/me/regions` 한 번 → 주소 이동 없이 같은 창에 폼이 열리고 사진이 맨 위·거래 동네 "역삼동 (대표)"·토스트 "역삼동을 인증했어요". 가격 칸에 1 → 12 → 123 → 12,345 → 지우기까지 NaN 없이 쉼표가 찍히고 빈 칸으로 돌아옴, 1000000 → 1,000,000. 거래 방식 두 칸 전환. **실제 위치 권한 창과 실제 인증은 사용자 확인 필요.**
- [완료] 창을 열면 닫기 버튼에 초점 링이 뜨던 문제(2026-09-21): 손가락·마우스로 쓰는 동안 버튼 초점 링 숨김.
- [검증] 2026-09-21 빌드 성공, 단위 테스트 99개 → 103개(입력 방식 4개: 처음 pointer · Tab ↔ 터치 전환 · 글자 입력은 키보드로 안 봄 · 해제). 브라우저에서 창을 열면 초점이 닫기 버튼으로 가는 것(원인)과 Tab·터치에 따라 `data-input` 이 바뀌는 것, 빌드 CSS 에 숨김 규칙이 들어간 것을 확인했다. 데스크톱 Chrome 은 이 초점에 원래 링을 그리지 않아 증상 자체는 재현되지 않는다 — **아이폰에서 사용자 확인 필요.**
- [완료] 아이폰 크롬 로그아웃 실패 수정(2026-09-22): `currentSubscription` 이 `pushManager` 없는 등록을 "구독 없음"으로 처리.
- [검증] 2026-09-22 단위 테스트 116개 → 117개(pushManager 없는 등록에서 해제·계정 전환이 오류 없이 끝남), 빌드 성공. 방어 줄을 빼면 새 테스트가 실패하는 것을 확인하고 되돌렸다. **실제 아이폰 크롬에서 로그아웃은 배포 뒤 사용자 확인 필요.**
- [완료] 모킹 E2E 복구(2026-09-22): 라우터·UI 변경 뒤 한 번도 돌지 않던 `tests/app.spec.js` 를 지금 화면에 맞췄다. "더 보기" → 무한 스크롤(끝까지 내리면 다음 페이지, 커서 요청 정확히 1회), 로그인 진입은 나의 골목 "로그인하기"(모바일 헤더에 로그인 없음), 로그아웃은 설정 화면(끝나면 `/my`, localStorage·sessionStorage 모두 비어야 함), 빈 검색 결과 문구, 위치 거부 문구. 화면이 함께 부르는 부가 API(인기 검색어·안 읽은 수·내 정보·푸시 공개키)를 목업에 넣었다 — 없으면 404 가 사이드바 오류 알림으로 떠 `getByRole("alert")` 가 두 개를 잡았다. 알림 확인은 해당 영역(상품 목록·동네 선택 창) 안으로 좁혔다.
- [검증] 2026-09-22 설치된 Chrome 으로 모킹 E2E **10개(PC 1440 · 모바일 390 × 5 시나리오) 모두 통과**(고치기 전 2 통과 · 8 실패 — 모두 테스트가 화면 변경을 따라가지 못한 것이었고 앱 버그는 없었다). 단위 테스트 117개.
- [완료] 실제 서버 E2E 복구(2026-09-22): 로그인 진입(나의 골목 "로그인하기"), 계정 전환(세션만 지우고 다시 로그인), 등록 버튼 이름("상품 등록"), 사진 순서(방향키 + 이동 안내 문구), 가격 쉼표 표시("18,000"), 상세 사진 여러 장, 빈 상태 문구, 보이는 쪽 버튼 고르기(`filter({ visible: true })`)로 맞췄다. 모킹 E2E 는 채팅 WebSocket 도 가로채도록 했다(`routeWebSocket`) — 8080 에 백엔드가 떠 있으면 가짜 토큰으로 실제 소켓에 붙어 거부당하고, 앱이 설계대로 세션을 지워 로그인 시나리오가 깨졌다.
- [검증] 2026-09-22 실제 서버 E2E **4개 모두 통과**(PC·모바일 × 상품 쓰기·후기): 사진 업로드 → 등록 → 끌어올리기 거부 → 수정·사진 삭제 → 예약중 → 판매내역 필터 → 판매완료 → 삭제(404), 후기 양쪽 작성·프로필 온도 36.7℃·받은 후기·중복 409, **실제 MySQL 8.0 에서 동시 후기 201/201/409 와 온도 37.7℃**. 모킹 E2E 10개는 백엔드가 떠 있을 때·없을 때 모두 통과. 단위 117개. 새 스키마 파일로 만든 DB 에서 백엔드 `ddl-auto: validate` 도 통과했다. 끝난 뒤 백엔드 프로세스·DB 컨테이너·임시 비밀값·업로드 폴더를 지웠다. 테스트 계정·상품은 일회용 DB 와 함께 사라졌다(사용자 DB·운영 무관).
- [다음] 미정. 후보는 상시 주소로 이전(Oracle Cloud).
