# 프론트에서 쓰는 값만 VITE_ 접두사. 실제 .env는 커밋하지 않는다.
VITE_API_BASE=/api
# 채팅 WebSocket 주소. 비우면 현재 페이지와 같은 호스트의 /api/ws (개발 서버 프록시).
# 배포(Vercel)에서는 rewrites 가 WebSocket 을 중계하지 못하므로 백엔드 주소를 직접 준다. 예: wss://api.example.com/api/ws
VITE_WS_URL=
